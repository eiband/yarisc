diff --git a/clang/lib/Basic/CMakeLists.txt b/clang/lib/Basic/CMakeLists.txt
index adfc6ee326b5..26a8654ccda9 100644
--- a/clang/lib/Basic/CMakeLists.txt
+++ b/clang/lib/Basic/CMakeLists.txt
@@ -122,6 +122,7 @@ add_clang_library(clangBasic
   Targets/X86.cpp
   Targets/XCore.cpp
   Targets/Xtensa.cpp
+  Targets/YaRISC.cpp
   TokenKinds.cpp
   TypeTraits.cpp
   Version.cpp
diff --git a/clang/lib/Basic/Targets.cpp b/clang/lib/Basic/Targets.cpp
index 38eb1edd4bfb..58cb99f68eef 100644
--- a/clang/lib/Basic/Targets.cpp
+++ b/clang/lib/Basic/Targets.cpp
@@ -40,6 +40,7 @@
 #include "Targets/X86.h"
 #include "Targets/XCore.h"
 #include "Targets/Xtensa.h"
+#include "Targets/Yarisc.h"
 #include "clang/Basic/Diagnostic.h"
 #include "clang/Basic/DiagnosticFrontend.h"
 #include "llvm/ADT/StringExtras.h"
@@ -781,6 +782,9 @@ std::unique_ptr<TargetInfo> AllocateTarget(const llvm::Triple &Triple,
 
   case llvm::Triple::xtensa:
     return std::make_unique<XtensaTargetInfo>(Triple, Opts);
+
+  case llvm::Triple::yarisc:
+    return std::make_unique<YaRISCTargetInfo>(Triple, Opts);
   }
 }
 } // namespace targets
diff --git a/clang/lib/Basic/Targets/YaRISC.cpp b/clang/lib/Basic/Targets/YaRISC.cpp
new file mode 100644
index 000000000000..adf87efbada8
--- /dev/null
+++ b/clang/lib/Basic/Targets/YaRISC.cpp
@@ -0,0 +1,74 @@
+//===--- YaRISC.cpp - Implement YaRISC target feature support -------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file implements YaRISC TargetInfo objects.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISC.h"
+#include "Targets.h"
+#include "clang/Basic/MacroBuilder.h"
+#include "llvm/ADT/StringSwitch.h"
+
+using namespace clang;
+using namespace clang::targets;
+
+const char *const YaRISCTargetInfo::GCCRegNames[] = {
+    "r0", "r1", "r2", "r3", "r4", "r5", "sp", "ip",
+};
+
+ArrayRef<const char *> YaRISCTargetInfo::getGCCRegNames() const {
+  return llvm::ArrayRef(GCCRegNames);
+}
+
+ArrayRef<TargetInfo::GCCRegAlias> YaRISCTargetInfo::getGCCRegAliases() const {
+  return llvm::ArrayRef<TargetInfo::GCCRegAlias>();
+}
+
+bool YaRISCTargetInfo::hasFeature(StringRef Feature) const {
+  return llvm::StringSwitch<bool>(Feature)
+      .Case("softfloat", SoftFloat)
+      .Case("yarisc", true)
+      .Default(false);
+}
+
+struct YaRISCCPUInfo {
+  llvm::StringLiteral Name;
+  YaRISCTargetInfo::CPUKind Kind;
+};
+
+static constexpr YaRISCCPUInfo CPUInfo[] = {
+    {{"min"}, YaRISCTargetInfo::CK_MINIMUM},
+    {{"v1"}, YaRISCTargetInfo::CK_V1},
+};
+
+YaRISCTargetInfo::CPUKind YaRISCTargetInfo::getCPUKind(StringRef Name) const {
+  const YaRISCCPUInfo *Item = llvm::find_if(
+      CPUInfo, [Name](const YaRISCCPUInfo &Info) { return Info.Name == Name; });
+
+  if (Item == std::end(CPUInfo))
+    return CK_GENERIC;
+
+  return Item->Kind;
+}
+
+void YaRISCTargetInfo::fillValidCPUList(
+    SmallVectorImpl<StringRef> &Values) const {
+  for (const YaRISCCPUInfo &Info : CPUInfo)
+    Values.push_back(Info.Name);
+}
+
+void YaRISCTargetInfo::getTargetDefines(const LangOptions &Opts,
+                                        MacroBuilder &Builder) const {
+  DefineStd(Builder, "yarisc", Opts);
+  Builder.defineMacro("__yarisc__");
+  Builder.defineMacro("__REGISTER_PREFIX__", "");
+
+  if (SoftFloat)
+    Builder.defineMacro("SOFT_FLOAT", "1");
+}
diff --git a/clang/lib/Basic/Targets/YaRISC.h b/clang/lib/Basic/Targets/YaRISC.h
new file mode 100644
index 000000000000..1109bfa924c2
--- /dev/null
+++ b/clang/lib/Basic/Targets/YaRISC.h
@@ -0,0 +1,115 @@
+//===--- YaRISC.h - declare YaRISC target feature support -------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file declares YaRISC TargetInfo objects.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_CLANG_LIB_BASIC_TARGETS_YARISC_H
+#define LLVM_CLANG_LIB_BASIC_TARGETS_YARISC_H
+
+#include "clang/Basic/TargetInfo.h"
+#include "clang/Basic/TargetOptions.h"
+#include "llvm/Support/Compiler.h"
+#include "llvm/TargetParser/Triple.h"
+
+namespace clang {
+namespace targets {
+
+class LLVM_LIBRARY_VISIBILITY YaRISCTargetInfo : public TargetInfo {
+  static const char *const GCCRegNames[];
+  bool SoftFloat;
+
+public:
+  YaRISCTargetInfo(const llvm::Triple &Triple, const TargetOptions &)
+      : TargetInfo(Triple), SoftFloat(false) {
+    TLSSupported = false;
+    IntWidth = 16;
+    IntAlign = 16;
+    LongWidth = 32;
+    LongAlign = 16;
+    LongLongWidth = 64;
+    LongLongAlign = 16;
+    FloatWidth = 32;
+    FloatAlign = 16;
+    DoubleWidth = LongDoubleWidth = 64;
+    DoubleAlign = LongDoubleAlign = 16;
+    PointerWidth = 16;
+    PointerAlign = 16;
+    SuitableAlign = 16;
+    SizeType = UnsignedShort;
+    IntPtrType = SignedShort;
+    IntMaxType = SignedLongLong;
+    PtrDiffType = SignedShort;
+    resetDataLayout();
+  }
+
+  bool hasBitIntType() const override { return true; }
+
+  int getEHDataRegisterNumber(unsigned RegNo) const override { return -1; }
+
+  bool handleTargetFeatures(std::vector<std::string> &Features,
+                            DiagnosticsEngine &Diags) override {
+    // Check if software floating point is enabled
+    if (llvm::is_contained(Features, "+soft-float"))
+      SoftFloat = true;
+    return true;
+  }
+
+  void getTargetDefines(const LangOptions &Opts,
+                        MacroBuilder &Builder) const override;
+
+  bool hasFeature(StringRef Feature) const override;
+
+  llvm::SmallVector<Builtin::InfosShard> getTargetBuiltins() const override {
+    return {};
+  }
+
+  BuiltinVaListKind getBuiltinVaListKind() const override {
+    return TargetInfo::VoidPtrBuiltinVaList;
+  }
+
+  ArrayRef<const char *> getGCCRegNames() const override;
+  ArrayRef<TargetInfo::GCCRegAlias> getGCCRegAliases() const override;
+
+  bool validateAsmConstraint(const char *&Name,
+                             TargetInfo::ConstraintInfo &info) const override {
+    return false;
+  }
+
+  std::string_view getClobbers() const override { return ""; }
+
+  enum CPUKind {
+    CK_GENERIC,
+    CK_MINIMUM,
+    CK_V1,
+  } CPU = CK_GENERIC;
+
+  CPUKind getCPUKind(StringRef Name) const;
+
+  bool isValidCPUName(StringRef Name) const override {
+    return getCPUKind(Name) != CK_GENERIC;
+  }
+
+  void fillValidCPUList(SmallVectorImpl<StringRef> &Values) const override;
+
+  bool setCPU(const std::string &Name) override {
+    CPU = getCPUKind(Name);
+    return CPU != CK_GENERIC;
+  }
+
+  std::pair<unsigned, unsigned> hardwareInterferenceSizes() const override {
+    // There are no caches in this simple architecture.
+    return std::make_pair(1, 1);
+  }
+};
+
+} // namespace targets
+} // namespace clang
+
+#endif // LLVM_CLANG_LIB_BASIC_TARGETS_YARISC_H
diff --git a/llvm/CMakeLists.txt b/llvm/CMakeLists.txt
index 6d94cbbcd255..cab68f33ea60 100644
--- a/llvm/CMakeLists.txt
+++ b/llvm/CMakeLists.txt
@@ -556,6 +556,7 @@ set(LLVM_ALL_EXPERIMENTAL_TARGETS
   DirectX
   M68k
   Xtensa
+  YaRISC
 )
 
 # List of targets with JIT support:
diff --git a/llvm/include/llvm/BinaryFormat/ELF.h b/llvm/include/llvm/BinaryFormat/ELF.h
index bfcf5dab4772..762483e322ee 100644
--- a/llvm/include/llvm/BinaryFormat/ELF.h
+++ b/llvm/include/llvm/BinaryFormat/ELF.h
@@ -1090,6 +1090,11 @@ enum : unsigned {
   EF_XTENSA_XT_LIT = 0x00000200,
 };
 
+// ELF Relocation type for YaRISC.
+enum {
+#include "ELFRelocs/YaRISC.def"
+};
+
 // ELF Relocation types for Xtensa
 enum {
 #include "ELFRelocs/Xtensa.def"
diff --git a/llvm/include/llvm/BinaryFormat/ELFRelocs/YaRISC.def b/llvm/include/llvm/BinaryFormat/ELFRelocs/YaRISC.def
new file mode 100644
index 000000000000..b11a91fda16c
--- /dev/null
+++ b/llvm/include/llvm/BinaryFormat/ELFRelocs/YaRISC.def
@@ -0,0 +1,10 @@
+
+#ifndef ELF_RELOC
+#error "ELF_RELOC must be defined"
+#endif
+
+ELF_RELOC(R_YARISC_NONE,         0)
+ELF_RELOC(R_YARISC_16,           1)
+ELF_RELOC(R_YARISC_PCREL16,      2)
+ELF_RELOC(R_YARISC_PCREL5_S1,    3)
+ELF_RELOC(R_YARISC_PCREL9_S1,    4)
diff --git a/llvm/include/llvm/TargetParser/Triple.h b/llvm/include/llvm/TargetParser/Triple.h
index e8aa43a2337c..2a06ca2cb5ba 100644
--- a/llvm/include/llvm/TargetParser/Triple.h
+++ b/llvm/include/llvm/TargetParser/Triple.h
@@ -91,6 +91,7 @@ public:
     x86_64,      // X86-64: amd64, x86_64
     xcore,       // XCore: xcore
     xtensa,      // Tensilica: Xtensa
+    yarisc,      // YaRISC
     nvptx,       // NVPTX: 32-bit
     nvptx64,     // NVPTX: 64-bit
     amdil,       // AMDIL
diff --git a/llvm/include/module.modulemap b/llvm/include/module.modulemap
index 48b4622232ff..836173b4c714 100644
--- a/llvm/include/module.modulemap
+++ b/llvm/include/module.modulemap
@@ -100,6 +100,7 @@ module LLVM_BinaryFormat {
     textual header "llvm/BinaryFormat/ELFRelocs/Sparc.def"
     textual header "llvm/BinaryFormat/ELFRelocs/SystemZ.def"
     textual header "llvm/BinaryFormat/ELFRelocs/VE.def"
+    textual header "llvm/BinaryFormat/ELFRelocs/YaRISC.def"
     textual header "llvm/BinaryFormat/ELFRelocs/x86_64.def"
     textual header "llvm/BinaryFormat/ELFRelocs/Xtensa.def"
     textual header "llvm/BinaryFormat/WasmRelocs.def"
diff --git a/llvm/lib/Target/YaRISC/AsmParser/CMakeLists.txt b/llvm/lib/Target/YaRISC/AsmParser/CMakeLists.txt
new file mode 100644
index 000000000000..0fa56449815e
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/AsmParser/CMakeLists.txt
@@ -0,0 +1,14 @@
+add_llvm_component_library(LLVMYaRISCAsmParser
+  YaRISCAsmParser.cpp
+
+  LINK_COMPONENTS
+  MC
+  MCParser
+  YaRISCDesc
+  YaRISCInfo
+  Support
+  TargetParser
+
+  ADD_TO_COMPONENT
+  YaRISC
+  )
diff --git a/llvm/lib/Target/YaRISC/AsmParser/YaRISCAsmParser.cpp b/llvm/lib/Target/YaRISC/AsmParser/YaRISCAsmParser.cpp
new file mode 100644
index 000000000000..7867d0e9e3d5
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/AsmParser/YaRISCAsmParser.cpp
@@ -0,0 +1,258 @@
+//===-- YaRISCAsmParser.cpp - YaRISC assembly to MCInst instructions ------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "MCTargetDesc/YaRISCInstPrinter.h"
+#include "MCTargetDesc/YaRISCMCAsmInfo.h"
+#include "MCTargetDesc/YaRISCMCTargetDesc.h"
+#include "TargetInfo/YaRISCTargetInfo.h"
+#include "llvm/ADT/SmallVector.h"
+#include "llvm/ADT/StringRef.h"
+#include "llvm/BinaryFormat/ELF.h"
+#include "llvm/MC/MCAsmMacro.h"
+#include "llvm/MC/MCContext.h"
+#include "llvm/MC/MCExpr.h"
+#include "llvm/MC/MCInst.h"
+#include "llvm/MC/MCInstBuilder.h"
+#include "llvm/MC/MCInstrInfo.h"
+#include "llvm/MC/MCObjectFileInfo.h"
+#include "llvm/MC/MCParser/AsmLexer.h"
+#include "llvm/MC/MCParser/MCAsmParser.h"
+#include "llvm/MC/MCParser/MCParsedAsmOperand.h"
+#include "llvm/MC/MCParser/MCTargetAsmParser.h"
+#include "llvm/MC/MCRegisterInfo.h"
+#include "llvm/MC/MCStreamer.h"
+#include "llvm/MC/MCSubtargetInfo.h"
+#include "llvm/MC/MCSymbol.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Casting.h"
+#include "llvm/Support/Compiler.h"
+#include "llvm/Support/ErrorHandling.h"
+#include "llvm/Support/MathExtras.h"
+#include "llvm/Support/SMLoc.h"
+#include "llvm/Support/raw_ostream.h"
+#include "llvm/TargetParser/Triple.h"
+#include <algorithm>
+#include <cassert>
+#include <cstdint>
+#include <memory>
+
+using namespace llvm;
+
+class YaRISCAsmParser : public MCTargetAsmParser {
+  MCAsmParser &Parser;
+  const MCRegisterInfo &MRI;
+
+  /// @name Auto-generated Match Functions
+  /// {
+
+#define GET_ASSEMBLER_HEADER
+#include "YaRISCGenAsmMatcher.inc"
+
+  /// }
+
+public:
+  YaRISCAsmParser(const MCSubtargetInfo &sti, MCAsmParser &parser,
+                  const MCInstrInfo &MII, const MCTargetOptions &Options)
+      : MCTargetAsmParser(Options, sti, MII), Parser(parser),
+        MRI(*Parser.getContext().getRegisterInfo()) {}
+
+  bool parseRegister(MCRegister &Reg, SMLoc &StartLoc, SMLoc &EndLoc) override;
+  ParseStatus tryParseRegister(MCRegister &Reg, SMLoc &StartLoc,
+                               SMLoc &EndLoc) override;
+
+  bool matchAndEmitInstruction(SMLoc IDLoc, unsigned &Opcode,
+                               OperandVector &Operands, MCStreamer &Out,
+                               uint64_t &ErrorInfo,
+                               bool MatchingInlineAsm) override;
+
+  bool parseInstruction(ParseInstructionInfo &Info, StringRef Name,
+                        SMLoc NameLoc, OperandVector &Operands) override;
+};
+
+namespace {
+class YaRISCOperand : public MCParsedAsmOperand {
+public:
+  enum KindTy { Token, Register, Immediate, Memory } Kind;
+
+  SMLoc StartLoc, EndLoc;
+
+  struct TokOp {
+    const char *Data;
+    unsigned Length;
+  };
+
+  struct RegOp {
+    MCRegister RegNum;
+  };
+
+  struct ImmOp {
+    const MCExpr *Val;
+  };
+
+  struct MemOp {
+    MCRegister BaseReg;
+    const MCExpr *Offset;
+  };
+
+  union {
+    TokOp Tok;
+    RegOp Reg;
+    ImmOp Imm;
+    MemOp Mem;
+  };
+
+  explicit YaRISCOperand(KindTy K) : Kind(K) {}
+
+  bool isToken() const override { return Kind == Token; }
+  bool isReg() const override { return Kind == Register; }
+  bool isImm() const override { return Kind == Immediate; }
+  bool isMem() const override { return Kind == Memory; }
+
+  MCRegister getReg() const override {
+    assert(isReg());
+    return Reg.RegNum;
+  }
+
+  const MCExpr *getImm() const {
+    assert(isImm());
+    return Imm.Val;
+  }
+
+  StringRef getToken() const {
+    assert(isToken());
+    return StringRef(Tok.Data, Tok.Length);
+  }
+
+  static std::unique_ptr<YaRISCOperand> CreateToken(StringRef Str, SMLoc S) {
+    auto Op = std::make_unique<YaRISCOperand>(Token);
+    Op->StartLoc = S;
+    Op->EndLoc = S;
+    return Op;
+  }
+
+  static std::unique_ptr<YaRISCOperand> CreateReg(MCRegister RegNum, SMLoc S,
+                                                  SMLoc E) {
+    auto Op = std::make_unique<YaRISCOperand>(Register);
+    Op->Reg.RegNum = RegNum;
+    Op->StartLoc = S;
+    Op->EndLoc = E;
+    return Op;
+  }
+
+  static std::unique_ptr<YaRISCOperand> CreateImm(const MCExpr *Val, SMLoc S,
+                                                  SMLoc E) {
+    auto Op = std::make_unique<YaRISCOperand>(Immediate);
+    Op->Imm.Val = Val;
+    Op->StartLoc = S;
+    Op->EndLoc = E;
+    return Op;
+  }
+
+  static std::unique_ptr<YaRISCOperand>
+  CreateMem(MCRegister BaseReg, const MCExpr *Offset, SMLoc S, SMLoc E) {
+    auto Op = std::make_unique<YaRISCOperand>(Memory);
+    Op->Mem.BaseReg = BaseReg;
+    Op->Mem.Offset = Offset;
+    Op->StartLoc = S;
+    Op->EndLoc = E;
+    return Op;
+  }
+
+  void print(raw_ostream &OS, const MCAsmInfo &) const override {
+    switch (Kind) {
+    case Token:
+      OS << Tok.Data;
+      break;
+    case Register:
+      OS << "Reg:" << YaRISCInstPrinter::getRegisterName(Reg.RegNum);
+      break;
+    case Immediate:
+      if (Imm.Val->getKind() == MCExpr::Constant) {
+        if (auto ImmExpr = cast<MCConstantExpr>(Imm.Val)->getValue())
+          OS << "Imm:" << ImmExpr;
+      } else if (Imm.Val->getKind() == MCExpr::SymbolRef) {
+        if (auto *SRE = dyn_cast<MCSymbolRefExpr>(Imm.Val)) {
+          const MCSymbol &Sym = SRE->getSymbol();
+          if (const char *SymNameStr = Sym.getName().data())
+            OS << "Imm:" << SymNameStr;
+        }
+      }
+      break;
+    case Memory:
+      // TODO: YaRISC
+      break;
+    }
+  }
+
+  SMLoc getStartLoc() const override { return StartLoc; }
+  SMLoc getEndLoc() const override { return EndLoc; }
+
+  void addExpr(MCInst &Inst, const MCExpr *Expr) const {
+    if (const MCConstantExpr *CE = dyn_cast<MCConstantExpr>(Expr))
+      Inst.addOperand(MCOperand::createImm(CE->getValue()));
+    else
+      Inst.addOperand(MCOperand::createExpr(Expr));
+  }
+
+  void addRegOperands(MCInst &Inst, unsigned N) const {
+    assert(N == 1 && "Invalid number of operands!");
+    Inst.addOperand(MCOperand::createReg(getReg()));
+  }
+
+  void addImmOperands(MCInst &Inst, unsigned N) const {
+    assert(N == 1 && "Invalid number of operands!");
+    addExpr(Inst, getImm());
+  }
+
+  void addMemOperands(MCInst &Inst, unsigned N) const {
+    assert(N >= 1 && N <= 2 && "Invalid number of operands!");
+    int64_t Val = 0;
+    if (!Mem.Offset->evaluateAsAbsolute(Val))
+      report_fatal_error("Non-absolute offset not supported yet");
+    if (N == 1) {
+      Inst.addOperand(MCOperand::createImm(Val));
+    } else {
+      Inst.addOperand(MCOperand::createReg(Mem.BaseReg));
+      Inst.addOperand(MCOperand::createImm(Val));
+    }
+  }
+};
+} // namespace
+
+#define GET_REGISTER_MATCHER
+#define GET_MATCHER_IMPLEMENTATION
+#include "YaRISCGenAsmMatcher.inc"
+
+bool YaRISCAsmParser::parseRegister(MCRegister &Reg, SMLoc &StartLoc,
+                                    SMLoc &EndLoc) {
+  llvm_unreachable("YaRISC asm parser not yet implemented!");
+}
+
+ParseStatus YaRISCAsmParser::tryParseRegister(MCRegister &Reg, SMLoc &StartLoc,
+                                              SMLoc &EndLoc) {
+  llvm_unreachable("YaRISC asm parser not yet implemented!");
+}
+
+bool YaRISCAsmParser::matchAndEmitInstruction(SMLoc IDLoc, unsigned &Opcode,
+                                              OperandVector &Operands,
+                                              MCStreamer &Out,
+                                              uint64_t &ErrorInfo,
+                                              bool MatchingInlineAsm) {
+  llvm_unreachable("YaRISC asm parser not yet implemented!");
+}
+
+bool YaRISCAsmParser::parseInstruction(ParseInstructionInfo &Info,
+                                       StringRef Name, SMLoc NameLoc,
+                                       OperandVector &Operands) {
+  llvm_unreachable("YaRISC asm parser not yet implemented!");
+}
+
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void
+LLVMInitializeYaRISCAsmParser() {
+  RegisterMCAsmParser<YaRISCAsmParser> A(getTheYaRISCTarget());
+}
diff --git a/llvm/lib/Target/YaRISC/CMakeLists.txt b/llvm/lib/Target/YaRISC/CMakeLists.txt
new file mode 100644
index 000000000000..1b6470e6103b
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/CMakeLists.txt
@@ -0,0 +1,50 @@
+add_llvm_component_group(YaRISC)
+
+set(LLVM_TARGET_DEFINITIONS YaRISC.td)
+
+tablegen(LLVM YaRISCGenAsmMatcher.inc -gen-asm-matcher)
+tablegen(LLVM YaRISCGenAsmWriter.inc -gen-asm-writer)
+tablegen(LLVM YaRISCGenCallingConv.inc -gen-callingconv)
+tablegen(LLVM YaRISCGenDAGISel.inc -gen-dag-isel)
+tablegen(LLVM YaRISCGenDisassemblerTables.inc -gen-disassembler)
+tablegen(LLVM YaRISCGenInstrInfo.inc -gen-instr-info)
+tablegen(LLVM YaRISCGenMCCodeEmitter.inc -gen-emitter)
+tablegen(LLVM YaRISCGenRegisterInfo.inc -gen-register-info)
+tablegen(LLVM YaRISCGenSDNodeInfo.inc -gen-sd-node-info)
+tablegen(LLVM YaRISCGenSearchableTables.inc -gen-searchable-tables)
+tablegen(LLVM YaRISCGenSubtargetInfo.inc -gen-subtarget)
+
+add_public_tablegen_target(YaRISCCommonTableGen)
+
+add_llvm_target(YaRISCCodeGen
+  YaRISCAsmPrinter.cpp
+  YaRISCInstrInfo.cpp
+  YaRISCISelDAGToDAG.cpp
+  YaRISCISelLowering.cpp
+  YaRISCFrameLowering.cpp
+  YaRISCRegisterInfo.cpp
+  YaRISCSelectionDAGInfo.cpp
+  YaRISCSubtarget.cpp
+  YaRISCTargetMachine.cpp
+
+  LINK_COMPONENTS
+  AsmPrinter
+  CodeGen
+  CodeGenTypes
+  Core
+  MC
+  SelectionDAG
+  YaRISCDesc
+  YaRISCInfo
+  Support
+  Target
+  TargetParser
+
+  ADD_TO_COMPONENT
+  YaRISC
+  )
+
+add_subdirectory(AsmParser)
+add_subdirectory(Disassembler)
+add_subdirectory(MCTargetDesc)
+add_subdirectory(TargetInfo)
diff --git a/llvm/lib/Target/YaRISC/Disassembler/CMakeLists.txt b/llvm/lib/Target/YaRISC/Disassembler/CMakeLists.txt
new file mode 100644
index 000000000000..bd256b290d2f
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/Disassembler/CMakeLists.txt
@@ -0,0 +1,12 @@
+add_llvm_component_library(LLVMYaRISCDisassembler
+  YaRISCDisassembler.cpp
+
+  LINK_COMPONENTS
+  MCDisassembler
+  MC
+  YaRISCInfo
+  Support
+
+  ADD_TO_COMPONENT
+  YaRISC
+  )
diff --git a/llvm/lib/Target/YaRISC/Disassembler/YaRISCDisassembler.cpp b/llvm/lib/Target/YaRISC/Disassembler/YaRISCDisassembler.cpp
new file mode 100644
index 000000000000..74b9d275ad97
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/Disassembler/YaRISCDisassembler.cpp
@@ -0,0 +1,64 @@
+//===- YaRISCDisassembler.cpp - Disassembler for YaRISC ---------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file is part of the YaRISC Disassembler.
+//
+//===----------------------------------------------------------------------===//
+
+#include "MCTargetDesc/YaRISCMCTargetDesc.h"
+#include "TargetInfo/YaRISCTargetInfo.h"
+#include "llvm/MC/MCAsmInfo.h"
+#include "llvm/MC/MCContext.h"
+#include "llvm/MC/MCDecoder.h"
+#include "llvm/MC/MCDecoderOps.h"
+#include "llvm/MC/MCDisassembler/MCDisassembler.h"
+#include "llvm/MC/MCInst.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Compiler.h"
+#include "llvm/Support/Endian.h"
+
+using namespace llvm;
+using namespace llvm::MCD;
+
+#define DEBUG_TYPE "yarisc-disassembler"
+
+typedef MCDisassembler::DecodeStatus DecodeStatus;
+
+namespace {
+class YaRISCDisassembler : public MCDisassembler {
+public:
+  YaRISCDisassembler(const MCSubtargetInfo &STI, MCContext &Ctx)
+      : MCDisassembler(STI, Ctx) {}
+
+  ~YaRISCDisassembler() override = default;
+
+  DecodeStatus getInstruction(MCInst &Instr, uint64_t &Size,
+                              ArrayRef<uint8_t> Bytes, uint64_t Address,
+                              raw_ostream &CStream) const override;
+};
+} // namespace
+
+static MCDisassembler *createYaRISCDisassembler(const Target &T,
+                                                const MCSubtargetInfo &STI,
+                                                MCContext &Ctx) {
+  return new YaRISCDisassembler(STI, Ctx);
+}
+
+DecodeStatus YaRISCDisassembler::getInstruction(MCInst &Instr, uint64_t &Size,
+                                                ArrayRef<uint8_t> Bytes,
+                                                uint64_t Address,
+                                                raw_ostream &CStream) const {
+  llvm_unreachable("YaRISC disassembler not yet implemented!");
+}
+
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void
+LLVMInitializeYaRISCDisassembler() {
+  // Register the disassembler.
+  TargetRegistry::RegisterMCDisassembler(getTheYaRISCTarget(),
+                                         createYaRISCDisassembler);
+}
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/CMakeLists.txt b/llvm/lib/Target/YaRISC/MCTargetDesc/CMakeLists.txt
new file mode 100644
index 000000000000..a6f0c4fec914
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/CMakeLists.txt
@@ -0,0 +1,18 @@
+add_llvm_component_library(LLVMYaRISCDesc
+  YaRISCAsmBackend.cpp
+  YaRISCELFObjectWriter.cpp
+  YaRISCInstPrinter.cpp
+  YaRISCMCAsmInfo.cpp
+  YaRISCMCCodeEmitter.cpp
+  YaRISCMCTargetDesc.cpp
+  YaRISCTargetStreamer.cpp
+
+  LINK_COMPONENTS
+  MC
+  YaRISCInfo
+  Support
+  TargetParser
+
+  ADD_TO_COMPONENT
+  YaRISC
+  )
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCAsmBackend.cpp b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCAsmBackend.cpp
new file mode 100644
index 000000000000..ebeba8a72538
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCAsmBackend.cpp
@@ -0,0 +1,185 @@
+//===-- YaRISCAsmBackend.cpp - YaRISC Assembler Backend -------------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "MCTargetDesc/YaRISCFixupKinds.h"
+#include "MCTargetDesc/YaRISCMCTargetDesc.h"
+#include "llvm/ADT/StringSwitch.h"
+#include "llvm/MC/MCAsmBackend.h"
+#include "llvm/MC/MCContext.h"
+#include "llvm/MC/MCELFObjectWriter.h"
+#include "llvm/MC/MCExpr.h"
+#include "llvm/MC/MCObjectWriter.h"
+#include "llvm/MC/MCSubtargetInfo.h"
+#include "llvm/MC/MCValue.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/EndianStream.h"
+#include "llvm/Support/MathExtras.h"
+
+using namespace llvm;
+
+namespace {
+class YaRISCAsmBackend : public MCAsmBackend {
+public:
+  explicit YaRISCAsmBackend(const MCSubtargetInfo &STI)
+      : MCAsmBackend(STI.getTargetTriple().isLittleEndian()
+                         ? llvm::endianness::little
+                         : llvm::endianness::big) {}
+
+  std::optional<MCFixupKind> getFixupKind(StringRef Name) const override;
+  MCFixupKindInfo getFixupKindInfo(MCFixupKind Kind) const override;
+  void applyFixup(const MCFragment &, const MCFixup &, const MCValue &Target,
+                  uint8_t *Data, uint64_t Value, bool IsResolved) override;
+
+  unsigned getMinimumNopSize() const override { return 2; }
+
+  bool writeNopData(raw_ostream &OS, uint64_t Count,
+                    const MCSubtargetInfo *STI) const override {
+    // If the count is not 2-byte aligned, we must be writing data into the text
+    // section otherwise we have unaligned instructions.
+    OS.write_zeros(Count % 2);
+
+    uint64_t NumNops = Count / 2;
+    for (uint64_t i = 0; i != NumNops; ++i)
+      support::endian::write<uint16_t>(OS, 0x003f, Endian);
+
+    return true;
+  }
+};
+
+class ELFYaRISCAsmBackend : public YaRISCAsmBackend {
+  Triple::OSType OSType;
+
+public:
+  ELFYaRISCAsmBackend(const MCSubtargetInfo &STI, Triple::OSType OSType)
+      : YaRISCAsmBackend(STI), OSType(OSType) {}
+
+  std::unique_ptr<MCObjectTargetWriter>
+  createObjectTargetWriter() const override {
+    uint8_t OSABI = MCELFObjectTargetWriter::getOSABI(OSType);
+    return createYaRISCELFObjectWriter(OSABI);
+  }
+};
+} // namespace
+
+static void checkFixupValue(SMLoc Loc, unsigned N, uint64_t Value,
+                            MCContext &Ctx) {
+  if (!isIntN(N, Value))
+    Ctx.reportError(Loc, "fixup value out of range [" + Twine(minIntN(N)) +
+                             ", " + Twine(maxIntN(N)) + "]");
+  if (Value % 2)
+    Ctx.reportError(Loc, "fixup value must be 2-byte aligned");
+}
+
+static uint64_t adjustFixupValue(const MCFixup &Fixup,
+                                 const MCFixupKindInfo &FKI, uint64_t Value,
+                                 MCContext &Ctx) {
+  switch (Fixup.getKind()) {
+  case YaRISC::fixup_yarisc_pcrel16:
+    // The branch address is relative to the next instruction.
+    Value -= 4;
+    checkFixupValue(Fixup.getLoc(), FKI.TargetSize, Value, Ctx);
+    break;
+  case YaRISC::fixup_yarisc_pcrel5_s1:
+  case YaRISC::fixup_yarisc_pcrel9_s1:
+    // The branch address is relative to the next instruction.
+    Value -= 2;
+    checkFixupValue(Fixup.getLoc(), FKI.TargetSize + 1, Value, Ctx);
+    // Apply S1 shift for scaled addresses.
+    Value >>= 1;
+    break;
+  default:
+    break;
+  }
+
+  return Value;
+}
+
+std::optional<MCFixupKind>
+YaRISCAsmBackend::getFixupKind(StringRef Name) const {
+  unsigned Type;
+  Type = llvm::StringSwitch<unsigned>(Name)
+#define ELF_RELOC(X, Y) .Case(#X, Y)
+#include "llvm/BinaryFormat/ELFRelocs/YaRISC.def"
+#undef ELF_RELOC
+             .Case("BFD_RELOC_NONE", ELF::R_YARISC_NONE)
+             .Case("BFD_RELOC_16", ELF::R_YARISC_16)
+             .Default(-1u);
+  if (Type == -1u)
+    return std::nullopt;
+  return static_cast<MCFixupKind>(FirstLiteralRelocationKind + Type);
+}
+
+MCFixupKindInfo YaRISCAsmBackend::getFixupKindInfo(MCFixupKind Kind) const {
+  assert(Endian == llvm::endianness::little && "Unexpected endianness");
+  // clang-format off
+  const static MCFixupKindInfo Infos[YaRISC::NumTargetFixupKinds] = {
+    // This table *must* be in same the order of fixup_* kinds in
+    // YaRISCFixupKinds.h.
+    //
+    // name                        offset  bits    flags
+    { "fixup_yarisc_16",           16,     16,     0 },
+    { "fixup_yarisc_pcrel16",      16,     16,     0 },
+    { "fixup_yarisc_pcrel5_s1",    10,     5,      0 },
+    { "fixup_yarisc_pcrel9_s1",    6,      9,      0 },
+  };
+  // clang-format on
+  static_assert((std::size(Infos)) == YaRISC::NumTargetFixupKinds,
+                "Not all fixup kinds added to Infos array");
+
+  // Fixup kinds from raw relocation types and .reloc directives force
+  // relocations and do not use these fields.
+  if (mc::isRelocation(Kind))
+    return {};
+
+  if (Kind < FirstTargetFixupKind)
+    return MCAsmBackend::getFixupKindInfo(Kind);
+
+  assert(unsigned(Kind - FirstTargetFixupKind) < YaRISC::NumTargetFixupKinds &&
+         "Invalid kind!");
+  return Infos[Kind - FirstTargetFixupKind];
+}
+
+void YaRISCAsmBackend::applyFixup(const MCFragment &F, const MCFixup &Fixup,
+                                  const MCValue &Target, uint8_t *Data,
+                                  uint64_t Value, bool IsResolved) {
+  assert(Endian == llvm::endianness::little && "Unexpected endianness");
+  maybeAddReloc(F, Fixup, Target, Value, IsResolved);
+  if (!IsResolved)
+    return;
+  if (!Value)
+    return;
+
+  const MCFixupKindInfo &FKI = getFixupKindInfo(Fixup.getKind());
+  unsigned TargetOffset = FKI.TargetOffset;
+  unsigned TargetSize = FKI.TargetSize;
+
+  if (TargetSize == 0)
+    return;
+
+  // Apply target-specific value adjustments.
+  Value = adjustFixupValue(Fixup, FKI, Value, getContext());
+
+  // Mask the value to the number of bits.
+  uint64_t Mask = (TargetSize == 64) ? ~0ULL : ((1ULL << TargetSize) - 1);
+  Value &= Mask;
+
+  // Shift into position.
+  Value <<= TargetOffset;
+
+  unsigned NumBytes = (TargetOffset + TargetSize + 7) / 8;
+
+  for (unsigned I = 0; I != NumBytes; ++I)
+    Data[I] |= uint8_t((Value >> (I * 8)) & 0xff);
+}
+
+MCAsmBackend *llvm::createYaRISCAsmBackend(const Target &T,
+                                           const MCSubtargetInfo &STI,
+                                           const MCRegisterInfo &MRI,
+                                           const MCTargetOptions &Options) {
+  return new ELFYaRISCAsmBackend(STI, STI.getTargetTriple().getOS());
+}
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCELFObjectWriter.cpp b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCELFObjectWriter.cpp
new file mode 100644
index 000000000000..9f3a82d57957
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCELFObjectWriter.cpp
@@ -0,0 +1,86 @@
+//===-- YaRISCELFObjectWriter.cpp - YaRISC ELF Writer ---------------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "MCTargetDesc/YaRISCFixupKinds.h"
+#include "MCTargetDesc/YaRISCMCTargetDesc.h"
+#include "llvm/MC/MCContext.h"
+#include "llvm/MC/MCELFObjectWriter.h"
+#include "llvm/MC/MCExpr.h"
+#include "llvm/MC/MCObjectFileInfo.h"
+#include "llvm/MC/MCObjectWriter.h"
+#include "llvm/MC/MCValue.h"
+#include "llvm/Support/ErrorHandling.h"
+
+using namespace llvm;
+
+namespace {
+class YaRISCELFObjectWriter : public MCELFObjectTargetWriter {
+public:
+  explicit YaRISCELFObjectWriter(uint8_t OSABI)
+      : MCELFObjectTargetWriter(
+            /*Is64Bit*/ false, OSABI, ELF::EM_NONE,
+            /*HasRelocationAddend*/ false) {}
+
+  ~YaRISCELFObjectWriter() override = default;
+
+protected:
+  unsigned getRelocType(const MCFixup &Fixup, const MCValue &Target,
+                        bool IsPCRel) const override;
+};
+} // namespace
+
+unsigned YaRISCELFObjectWriter::getRelocType(const MCFixup &Fixup,
+                                             const MCValue &Target,
+                                             bool IsPCRel) const {
+  auto Kind = Fixup.getKind();
+  auto Spec = Target.getSpecifier();
+
+  if (mc::isRelocation(Kind))
+    return Kind;
+
+  switch (Spec) {
+  case ELF::R_YARISC_16:
+  case ELF::R_YARISC_PCREL16:
+  case ELF::R_YARISC_PCREL5_S1:
+  case ELF::R_YARISC_PCREL9_S1:
+    return Spec;
+  default:
+    break;
+  }
+
+  if (IsPCRel) {
+    switch (Kind) {
+    default:
+      llvm_unreachable("Unimplemented fixup -> relocation");
+    case FK_Data_2:
+    case YaRISC::fixup_yarisc_pcrel16:
+      return ELF::R_YARISC_PCREL16;
+    case YaRISC::fixup_yarisc_pcrel5_s1:
+      return ELF::R_YARISC_PCREL5_S1;
+    case YaRISC::fixup_yarisc_pcrel9_s1:
+      return ELF::R_YARISC_PCREL9_S1;
+    }
+  }
+
+  switch (Kind) {
+  default:
+    llvm_unreachable("Unimplemented fixup -> relocation");
+  case FK_NONE:
+    return ELF::R_YARISC_NONE;
+  case FK_Data_2:
+  case YaRISC::fixup_yarisc_16:
+    return ELF::R_YARISC_16;
+  }
+
+  return ELF::R_YARISC_NONE;
+}
+
+std::unique_ptr<MCObjectTargetWriter>
+llvm::createYaRISCELFObjectWriter(uint8_t OSABI) {
+  return std::make_unique<YaRISCELFObjectWriter>(OSABI);
+}
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCFixupKinds.h b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCFixupKinds.h
new file mode 100644
index 000000000000..ca496034f245
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCFixupKinds.h
@@ -0,0 +1,29 @@
+//===-- YaRISCFixupKinds.h - YaRISC Specific Fixup Entries ------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCFIXUPKINDS_H
+#define LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCFIXUPKINDS_H
+
+#include "llvm/MC/MCFixup.h"
+
+namespace llvm {
+namespace YaRISC {
+enum Fixups {
+  fixup_yarisc_16 = FirstTargetFixupKind,
+  fixup_yarisc_pcrel16,   // branch offset
+  fixup_yarisc_pcrel5_s1, // short conditional branch offset
+  fixup_yarisc_pcrel9_s1, // short unconditional branch offset
+
+  // Marker
+  LastTargetFixupKind,
+  NumTargetFixupKinds = LastTargetFixupKind - FirstTargetFixupKind
+};
+} // namespace YaRISC
+} // namespace llvm
+
+#endif
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCInstPrinter.cpp b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCInstPrinter.cpp
new file mode 100644
index 000000000000..d7d25e21ed53
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCInstPrinter.cpp
@@ -0,0 +1,68 @@
+//===-- YaRISCInstPrinter.cpp - Convert YaRISC MCInst to assembly syntax ---==//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This class prints an YaRISC MCInst to a .s file.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCInstPrinter.h"
+#include "YaRISC.h"
+#include "llvm/MC/MCAsmInfo.h"
+#include "llvm/MC/MCExpr.h"
+#include "llvm/MC/MCInst.h"
+#include "llvm/MC/MCSubtargetInfo.h"
+#include "llvm/Support/raw_ostream.h"
+using namespace llvm;
+
+#define DEBUG_TYPE "yarisc-asm-printer"
+
+#define GET_INSTRUCTION_NAME
+#define PRINT_ALIAS_INSTR
+#include "YaRISCGenAsmWriter.inc"
+
+void YaRISCInstPrinter::printRegName(raw_ostream &OS, MCRegister Reg) {
+  OS << getRegisterName(Reg);
+}
+
+void YaRISCInstPrinter::printInst(const MCInst *MI, uint64_t Address,
+                                  StringRef Annot, const MCSubtargetInfo &STI,
+                                  raw_ostream &O) {
+  if (!printAliasInstr(MI, Address, STI, O))
+    printInstruction(MI, Address, STI, O);
+  printAnnotation(O, Annot);
+}
+
+void YaRISCInstPrinter::printOperand(const MCInst *MI, int opNum,
+                                     const MCSubtargetInfo &STI,
+                                     raw_ostream &O) {
+  const MCOperand &MO = MI->getOperand(opNum);
+
+  if (MO.isReg()) {
+    printRegName(O, MO.getReg());
+    return;
+  }
+
+  if (MO.isImm()) {
+    markup(O, Markup::Immediate) << formatImm(MO.getImm());
+    return;
+  }
+
+  assert(MO.isExpr() && "Unknown operand kind in printOperand");
+  MAI.printExpr(O, *MO.getExpr());
+}
+
+void YaRISCInstPrinter::printCondCode(const MCInst *MI, unsigned OpNum,
+                                      const MCSubtargetInfo &STI,
+                                      raw_ostream &O) {
+  uint8_t Imm = MI->getOperand(OpNum).getImm();
+  auto Info = YaRISCCC::lookupMnemonicByEncoding(Imm);
+  if (Info)
+    O << Info->Mnemonic;
+  else
+    llvm_unreachable("Invalid condition code argument!");
+}
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCInstPrinter.h b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCInstPrinter.h
new file mode 100644
index 000000000000..5f3ab023cf69
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCInstPrinter.h
@@ -0,0 +1,54 @@
+//===-- YaRISCInstPrinter.h - Convert YaRISC MCInst to assembly syntax ----===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This class prints an YaRISC MCInst to a .s file.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCINSTPRINTER_H
+#define LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCINSTPRINTER_H
+
+#include "YaRISCMCTargetDesc.h"
+#include "llvm/MC/MCInstPrinter.h"
+
+namespace llvm {
+
+class YaRISCInstPrinter : public MCInstPrinter {
+public:
+  YaRISCInstPrinter(const MCAsmInfo &MAI, const MCInstrInfo &MII,
+                    const MCRegisterInfo &MRI)
+      : MCInstPrinter(MAI, MII, MRI) {}
+
+  void printRegName(raw_ostream &OS, MCRegister Reg) override;
+  void printInst(const MCInst *MI, uint64_t Address, StringRef Annot,
+                 const MCSubtargetInfo &STI, raw_ostream &O) override;
+
+  // Autogenerated by tablegen.
+  std::pair<const char *, uint64_t>
+  getMnemonic(const MCInst &MI) const override;
+  void printInstruction(const MCInst *MI, uint64_t Address,
+                        const MCSubtargetInfo &STI, raw_ostream &O);
+  bool printAliasInstr(const MCInst *MI, uint64_t Address,
+                       const MCSubtargetInfo &STI, raw_ostream &O);
+  static const char *getRegisterName(MCRegister Reg);
+
+  void printOperand(const MCInst *MI, int opNum, const MCSubtargetInfo &STI,
+                    raw_ostream &OS);
+  void printOperand(const MCInst *MI, uint64_t /*Address*/, unsigned OpNum,
+                    const MCSubtargetInfo &STI, raw_ostream &O) {
+    printOperand(MI, OpNum, STI, O);
+  }
+
+protected:
+  void printCondCode(const MCInst *MI, unsigned OpNum,
+                     const MCSubtargetInfo &STI, raw_ostream &O);
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCINSTPRINTER_H
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCAsmInfo.cpp b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCAsmInfo.cpp
new file mode 100644
index 000000000000..5a514557b909
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCAsmInfo.cpp
@@ -0,0 +1,66 @@
+//===- YaRISCMCAsmInfo.cpp - YaRISC asm properties ------------------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains the declarations of the YaRISCMCAsmInfo properties.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCMCAsmInfo.h"
+#include "llvm/ADT/StringSwitch.h"
+#include "llvm/BinaryFormat/Dwarf.h"
+#include "llvm/BinaryFormat/ELF.h"
+#include "llvm/MC/MCExpr.h"
+#include "llvm/MC/MCStreamer.h"
+#include "llvm/TargetParser/Triple.h"
+
+using namespace llvm;
+
+void YaRISCELFMCAsmInfo::anchor() {}
+
+YaRISCELFMCAsmInfo::YaRISCELFMCAsmInfo(const Triple &TheTriple) {
+  CodePointerSize = CalleeSaveStackSlotSize = 2;
+  MinInstAlignment = 2;
+  MaxInstLength = 4;
+}
+
+void YaRISCELFMCAsmInfo::printSpecifierExpr(raw_ostream &OS,
+                                            const MCSpecifierExpr &Expr) const {
+  auto S = Expr.getSpecifier();
+  bool HasSpecifier = S != 0 && S != ELF::R_YARISC_16;
+  if (HasSpecifier)
+    OS << '%' << YaRISC::getSpecifierName(S) << '(';
+  printExpr(OS, *Expr.getSubExpr());
+  if (HasSpecifier)
+    OS << ')';
+}
+
+StringRef YaRISC::getSpecifierName(uint16_t S) {
+  switch (S) {
+  default:
+    llvm_unreachable("Unhandled YaRISC specifier");
+  case 0:
+    return {};
+  case ELF::R_YARISC_16:
+    return "16";
+  case ELF::R_YARISC_PCREL16:
+    return "pcrel16";
+  case ELF::R_YARISC_PCREL5_S1:
+    return "pcrel5_s1";
+  case ELF::R_YARISC_PCREL9_S1:
+    return "pcrel9_s1";
+  }
+}
+
+uint16_t YaRISC::parseSpecifier(StringRef name) {
+  return StringSwitch<uint16_t>(name)
+      .Case("16", ELF::R_YARISC_16)
+      .Case("pcrel16", ELF::R_YARISC_PCREL16)
+      .Case("pcrel5_s1", ELF::R_YARISC_PCREL5_S1)
+      .Case("pcrel9_s1", ELF::R_YARISC_PCREL9_S1)
+      .Default(0);
+}
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCAsmInfo.h b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCAsmInfo.h
new file mode 100644
index 000000000000..dde0d03ac46f
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCAsmInfo.h
@@ -0,0 +1,39 @@
+//===- YaRISCMCAsmInfo.h - YaRISC asm properties ---------------*- C++ -*--===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains the declaration of the YaRISCMCAsmInfo class.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCMCASMINFO_H
+#define LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCMCASMINFO_H
+
+#include "llvm/MC/MCAsmInfoELF.h"
+
+namespace llvm {
+
+class Triple;
+
+class YaRISCELFMCAsmInfo : public MCAsmInfoELF {
+  void anchor() override;
+
+public:
+  explicit YaRISCELFMCAsmInfo(const Triple &TheTriple);
+
+  void printSpecifierExpr(raw_ostream &OS,
+                          const MCSpecifierExpr &Expr) const override;
+};
+
+namespace YaRISC {
+uint16_t parseSpecifier(StringRef name);
+StringRef getSpecifierName(uint16_t S);
+} // namespace YaRISC
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCMCASMINFO_H
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCCodeEmitter.cpp b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCCodeEmitter.cpp
new file mode 100644
index 000000000000..b9bcde8a858f
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCCodeEmitter.cpp
@@ -0,0 +1,200 @@
+//===-- YaRISCMCCodeEmitter.cpp - Convert YaRISC code to machine code -----===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file implements the YaRISCMCCodeEmitter class.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCFixupKinds.h"
+#include "YaRISCMCTargetDesc.h"
+#include "llvm/ADT/SmallVector.h"
+#include "llvm/ADT/Statistic.h"
+#include "llvm/BinaryFormat/ELF.h"
+#include "llvm/MC/MCAsmInfo.h"
+#include "llvm/MC/MCCodeEmitter.h"
+#include "llvm/MC/MCContext.h"
+#include "llvm/MC/MCExpr.h"
+#include "llvm/MC/MCFixup.h"
+#include "llvm/MC/MCInst.h"
+#include "llvm/MC/MCInstrInfo.h"
+#include "llvm/MC/MCObjectFileInfo.h"
+#include "llvm/MC/MCRegisterInfo.h"
+#include "llvm/MC/MCSubtargetInfo.h"
+#include "llvm/MC/MCSymbol.h"
+#include "llvm/Support/Casting.h"
+#include "llvm/Support/EndianStream.h"
+#include "llvm/Support/MathExtras.h"
+#include <cassert>
+#include <cstdint>
+
+using namespace llvm;
+
+#define DEBUG_TYPE "yarisc-mccodeemitter"
+
+STATISTIC(MCNumEmitted, "Number of MC instructions emitted");
+
+namespace {
+class YaRISCMCCodeEmitter : public MCCodeEmitter {
+  const MCInstrInfo &MCII;
+  MCContext &Ctx;
+
+public:
+  YaRISCMCCodeEmitter(const MCInstrInfo &mcii, MCContext &ctx)
+      : MCII(mcii), Ctx(ctx) {}
+
+  YaRISCMCCodeEmitter(const YaRISCMCCodeEmitter &) = delete;
+  YaRISCMCCodeEmitter &operator=(const YaRISCMCCodeEmitter &) = delete;
+
+  ~YaRISCMCCodeEmitter() override = default;
+
+  void encodeInstruction(const MCInst &MI, SmallVectorImpl<char> &CB,
+                         SmallVectorImpl<MCFixup> &Fixups,
+                         const MCSubtargetInfo &STI) const override;
+
+  // getBinaryCodeForInstr - TableGen'erated function for getting the
+  // binary encoding for an instruction.
+  uint64_t getBinaryCodeForInstr(const MCInst &MI,
+                                 SmallVectorImpl<MCFixup> &Fixups,
+                                 const MCSubtargetInfo &STI) const;
+
+  /// getMachineOpValue - Return binary encoding of operand. If the machine
+  /// operand requires relocation, record the relocation and return zero.
+  unsigned getMachineOpValue(const MCInst &MI, const MCOperand &MO,
+                             SmallVectorImpl<MCFixup> &Fixups,
+                             const MCSubtargetInfo &STI) const;
+
+  unsigned getImmOpValue(const MCInst &MI, unsigned OpNo,
+                         SmallVectorImpl<MCFixup> &Fixups,
+                         const MCSubtargetInfo &STI) const;
+  template <int N, int S = 0>
+  unsigned getBranchTargetOpValue(const MCInst &MI, unsigned OpNo,
+                                  SmallVectorImpl<MCFixup> &Fixups,
+                                  const MCSubtargetInfo &STI) const;
+};
+} // namespace
+
+static void addFixup(SmallVectorImpl<MCFixup> &Fixups, uint32_t Offset,
+                     const MCExpr *Value, uint16_t Kind) {
+  bool PCRel = false;
+  switch (Kind) {
+  default:
+    llvm_unreachable("unknown fixup kind");
+  case YaRISC::fixup_yarisc_16:
+    break;
+  case YaRISC::fixup_yarisc_pcrel16:
+  case YaRISC::fixup_yarisc_pcrel5_s1:
+  case YaRISC::fixup_yarisc_pcrel9_s1:
+    PCRel = true;
+    break;
+  }
+  Fixups.push_back(MCFixup::create(Offset, Value, Kind, PCRel));
+}
+
+void YaRISCMCCodeEmitter::encodeInstruction(const MCInst &MI,
+                                            SmallVectorImpl<char> &CB,
+                                            SmallVectorImpl<MCFixup> &Fixups,
+                                            const MCSubtargetInfo &STI) const {
+  uint64_t Binary = getBinaryCodeForInstr(MI, Fixups, STI);
+  unsigned Size = MCII.get(MI.getOpcode()).getSize();
+  auto Endianness = Ctx.getAsmInfo()->isLittleEndian()
+                        ? llvm::endianness::little
+                        : llvm::endianness::big;
+
+  switch (Size) {
+  default:
+    llvm_unreachable("Unexpected instruction size");
+  case 2:
+    support::endian::write<uint16_t>(CB, Binary, Endianness);
+    break;
+  case 4:
+    support::endian::write<uint32_t>(CB, Binary, Endianness);
+    break;
+  }
+
+  ++MCNumEmitted;
+}
+
+unsigned
+YaRISCMCCodeEmitter::getMachineOpValue(const MCInst &MI, const MCOperand &MO,
+                                       SmallVectorImpl<MCFixup> &Fixups,
+                                       const MCSubtargetInfo &STI) const {
+  if (MO.isReg())
+    return Ctx.getRegisterInfo()->getEncodingValue(MO.getReg());
+
+  if (MO.isImm())
+    return MO.getImm();
+
+  llvm_unreachable("Unhandled expression!");
+  return 0;
+}
+
+unsigned YaRISCMCCodeEmitter::getImmOpValue(const MCInst &MI, unsigned OpNo,
+                                            SmallVectorImpl<MCFixup> &Fixups,
+                                            const MCSubtargetInfo &STI) const {
+  const MCOperand &MO = MI.getOperand(OpNo);
+
+  if (MO.isImm())
+    return MO.getImm();
+
+  assert(MO.isExpr() && "getImmOpValue expects only expressions or immediates");
+  const MCExpr *Expr = MO.getExpr();
+  MCExpr::ExprKind Kind = Expr->getKind();
+
+  unsigned FixupKind = YaRISC::LastTargetFixupKind;
+  if (Kind == MCExpr::Specifier) {
+    const auto *RVExpr = cast<MCSpecifierExpr>(Expr);
+    FixupKind = RVExpr->getSpecifier();
+    switch (RVExpr->getSpecifier()) {
+    default:
+      assert(FixupKind && FixupKind < FirstTargetFixupKind &&
+             "invalid specifier");
+      break;
+    case ELF::R_YARISC_16:
+      FixupKind = YaRISC::fixup_yarisc_16;
+      break;
+    case ELF::R_YARISC_PCREL16:
+      FixupKind = YaRISC::fixup_yarisc_pcrel16;
+      break;
+    case ELF::R_YARISC_PCREL5_S1:
+      FixupKind = YaRISC::fixup_yarisc_pcrel5_s1;
+      break;
+    case ELF::R_YARISC_PCREL9_S1:
+      FixupKind = YaRISC::fixup_yarisc_pcrel9_s1;
+      break;
+    }
+  } else if (Kind == MCExpr::SymbolRef || Kind == MCExpr::Binary) {
+    FixupKind = YaRISC::fixup_yarisc_16;
+  }
+
+  assert(FixupKind != YaRISC::LastTargetFixupKind && "Unhandled expression!");
+  addFixup(Fixups, 0, Expr, FixupKind);
+  return 0;
+}
+
+template <int N, int S>
+unsigned
+YaRISCMCCodeEmitter::getBranchTargetOpValue(const MCInst &MI, unsigned OpNo,
+                                            SmallVectorImpl<MCFixup> &Fixups,
+                                            const MCSubtargetInfo &STI) const {
+  const MCOperand &MO = MI.getOperand(OpNo);
+
+  if (MO.isImm()) {
+    uint64_t Res = MO.getImm();
+    assert((isShiftedInt<N, S>(Res)) && "Immediate too large or not aligned");
+    return Res >> S;
+  }
+
+  return getImmOpValue(MI, OpNo, Fixups, STI);
+}
+
+#include "YaRISCGenMCCodeEmitter.inc"
+
+MCCodeEmitter *llvm::createYaRISCMCCodeEmitter(const MCInstrInfo &MCII,
+                                               MCContext &Ctx) {
+  return new YaRISCMCCodeEmitter(MCII, Ctx);
+}
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCTargetDesc.cpp b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCTargetDesc.cpp
new file mode 100644
index 000000000000..ae8f5b3b1b80
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCTargetDesc.cpp
@@ -0,0 +1,134 @@
+//===-- YaRISCMCTargetDesc.cpp - YaRISC Target Descriptions ---------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file provides YaRISC specific target descriptions.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCMCTargetDesc.h"
+#include "TargetInfo/YaRISCTargetInfo.h"
+#include "YaRISC.h"
+#include "YaRISCInstPrinter.h"
+#include "YaRISCMCAsmInfo.h"
+#include "YaRISCTargetStreamer.h"
+#include "llvm/MC/MCInstrInfo.h"
+#include "llvm/MC/MCRegisterInfo.h"
+#include "llvm/MC/MCSubtargetInfo.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Compiler.h"
+#include "llvm/Support/ErrorHandling.h"
+
+using namespace llvm;
+
+#define GET_INSTRINFO_MC_DESC
+#define ENABLE_INSTR_PREDICATE_VERIFIER
+#include "YaRISCGenInstrInfo.inc"
+
+#define GET_SUBTARGETINFO_MC_DESC
+#include "YaRISCGenSubtargetInfo.inc"
+
+#define GET_REGINFO_MC_DESC
+#include "YaRISCGenRegisterInfo.inc"
+
+namespace llvm {
+namespace YaRISCCC {
+#define GET_CC_VALTable_IMPL
+#include "YaRISCGenSearchableTables.inc"
+} // namespace YaRISCCC
+} // namespace llvm
+
+static MCAsmInfo *createYaRISCMCAsmInfo(const MCRegisterInfo &MRI,
+                                        const Triple &TT,
+                                        const MCTargetOptions &Options) {
+  MCAsmInfo *MAI = new YaRISCELFMCAsmInfo(TT);
+  // Initial state of the frame pointer is SP.
+  unsigned Reg = MRI.getDwarfRegNum(YaRISC::SP, true);
+  MAI->addInitialFrameState(MCCFIInstruction::cfiDefCfa(nullptr, Reg, 0));
+  return MAI;
+}
+
+static MCInstrInfo *createYaRISCMCInstrInfo() {
+  MCInstrInfo *X = new MCInstrInfo();
+  InitYaRISCMCInstrInfo(X);
+  return X;
+}
+
+static MCRegisterInfo *createYaRISCMCRegisterInfo(const Triple &TT) {
+  MCRegisterInfo *X = new MCRegisterInfo();
+  InitYaRISCMCRegisterInfo(X, YaRISC::NoRegister, 0, 0, YaRISC::IP);
+  return X;
+}
+
+static MCSubtargetInfo *
+createYaRISCMCSubtargetInfo(const Triple &TT, StringRef CPU, StringRef FS) {
+  if (CPU.empty())
+    CPU = "minimal";
+
+  MCSubtargetInfo *STI =
+      createYaRISCMCSubtargetInfoImpl(TT, CPU, /*TuneCPU=*/CPU, FS);
+
+  return STI;
+}
+
+static MCTargetStreamer *
+createObjectTargetStreamer(MCStreamer &S, const MCSubtargetInfo &STI) {
+  return new YaRISCTargetELFStreamer(S, STI);
+}
+
+static MCTargetStreamer *createTargetAsmStreamer(MCStreamer &S,
+                                                 formatted_raw_ostream &OS,
+                                                 MCInstPrinter *InstPrint) {
+  return new YaRISCTargetAsmStreamer(S, OS);
+}
+
+static MCTargetStreamer *createNullTargetStreamer(MCStreamer &S) {
+  return new YaRISCTargetStreamer(S);
+}
+
+static MCInstPrinter *createYaRISCMCInstPrinter(const Triple &T,
+                                                unsigned SyntaxVariant,
+                                                const MCAsmInfo &MAI,
+                                                const MCInstrInfo &MII,
+                                                const MCRegisterInfo &MRI) {
+  return new YaRISCInstPrinter(MAI, MII, MRI);
+}
+
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void
+LLVMInitializeYaRISCTargetMC() {
+  Target &T = getTheYaRISCTarget();
+
+  // Register the MC asm info.
+  RegisterMCAsmInfoFn X(T, createYaRISCMCAsmInfo);
+
+  // Register the MC instruction info.
+  TargetRegistry::RegisterMCInstrInfo(T, createYaRISCMCInstrInfo);
+
+  // Register the MC register info.
+  TargetRegistry::RegisterMCRegInfo(T, createYaRISCMCRegisterInfo);
+
+  // Register the MC subtarget info.
+  TargetRegistry::RegisterMCSubtargetInfo(T, createYaRISCMCSubtargetInfo);
+
+  // Register the MC Code Emitter.
+  TargetRegistry::RegisterMCCodeEmitter(T, createYaRISCMCCodeEmitter);
+
+  // Register the asm backend.
+  TargetRegistry::RegisterMCAsmBackend(T, createYaRISCAsmBackend);
+
+  // Register the object target streamer.
+  TargetRegistry::RegisterObjectTargetStreamer(T, createObjectTargetStreamer);
+
+  // Register the asm streamer.
+  TargetRegistry::RegisterAsmTargetStreamer(T, createTargetAsmStreamer);
+
+  // Register the null streamer.
+  TargetRegistry::RegisterNullTargetStreamer(T, createNullTargetStreamer);
+
+  // Register the MCInstPrinter
+  TargetRegistry::RegisterMCInstPrinter(T, createYaRISCMCInstPrinter);
+}
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCTargetDesc.h b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCTargetDesc.h
new file mode 100644
index 000000000000..3128a533af0a
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCMCTargetDesc.h
@@ -0,0 +1,68 @@
+//===-- YaRISCMCTargetDesc.h - YaRISC Target Descriptions -------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file provides YaRISC specific target descriptions.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCMCTARGETDESC_H
+#define LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCMCTARGETDESC_H
+
+#include "llvm/ADT/StringRef.h"
+#include "llvm/Support/DataTypes.h"
+
+#include <memory>
+
+namespace llvm {
+class MCAsmBackend;
+class MCCodeEmitter;
+class MCContext;
+class MCInstrInfo;
+class MCObjectTargetWriter;
+class MCRegisterInfo;
+class MCSubtargetInfo;
+class MCTargetOptions;
+class Target;
+class Triple;
+
+namespace YaRISCCC {
+struct CondCodeInfo {
+  uint8_t Encoding;
+  const char *Mnemonic;
+};
+
+#define GET_CC_VALTable_DECL
+#include "YaRISCGenSearchableTables.inc"
+} // namespace YaRISCCC
+
+MCCodeEmitter *createYaRISCMCCodeEmitter(const MCInstrInfo &MCII,
+                                         MCContext &Ctx);
+MCAsmBackend *createYaRISCAsmBackend(const Target &T,
+                                     const MCSubtargetInfo &STI,
+                                     const MCRegisterInfo &MRI,
+                                     const MCTargetOptions &Options);
+std::unique_ptr<MCObjectTargetWriter>
+createYaRISCELFObjectWriter(uint8_t OSABI);
+} // namespace llvm
+
+// Defines symbolic names for YaRISC registers. This defines a mapping from
+// register name to register number.
+//
+#define GET_REGINFO_ENUM
+#include "YaRISCGenRegisterInfo.inc"
+
+// Defines symbolic names for the YaRISC instructions.
+//
+#define GET_INSTRINFO_ENUM
+#define GET_INSTRINFO_MC_HELPER_DECLS
+#include "YaRISCGenInstrInfo.inc"
+
+#define GET_SUBTARGETINFO_ENUM
+#include "YaRISCGenSubtargetInfo.inc"
+
+#endif // LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCMCTARGETDESC_H
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCTargetStreamer.cpp b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCTargetStreamer.cpp
new file mode 100644
index 000000000000..2ddcccd745d1
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCTargetStreamer.cpp
@@ -0,0 +1,33 @@
+//===-- YaRISCTargetStreamer.cpp - YaRISC Target Streamer Methods ---------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file provides YaRISC specific target streamer methods.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCTargetStreamer.h"
+
+using namespace llvm;
+
+// Pin vtable to this file.
+void YaRISCTargetStreamer::anchor() {}
+
+YaRISCTargetStreamer::YaRISCTargetStreamer(MCStreamer &S)
+    : MCTargetStreamer(S) {}
+
+YaRISCTargetAsmStreamer::YaRISCTargetAsmStreamer(MCStreamer &S,
+                                                 formatted_raw_ostream &OS)
+    : YaRISCTargetStreamer(S), OS(OS) {}
+
+YaRISCTargetELFStreamer::YaRISCTargetELFStreamer(MCStreamer &S,
+                                                 const MCSubtargetInfo &STI)
+    : YaRISCTargetStreamer(S) {}
+
+MCELFStreamer &YaRISCTargetELFStreamer::getStreamer() {
+  return static_cast<MCELFStreamer &>(Streamer);
+}
diff --git a/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCTargetStreamer.h b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCTargetStreamer.h
new file mode 100644
index 000000000000..f52085a768a0
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/MCTargetDesc/YaRISCTargetStreamer.h
@@ -0,0 +1,43 @@
+//===-- YaRISCTargetStreamer.h - YaRISC Target Streamer --------*- C++ -*--===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCTARGETSTREAMER_H
+#define LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCTARGETSTREAMER_H
+
+#include "llvm/MC/MCELFStreamer.h"
+#include "llvm/MC/MCStreamer.h"
+
+namespace llvm {
+
+class formatted_raw_ostream;
+
+class YaRISCTargetStreamer : public MCTargetStreamer {
+  virtual void anchor();
+
+public:
+  explicit YaRISCTargetStreamer(MCStreamer &S);
+};
+
+// This part is for ascii assembly output
+class YaRISCTargetAsmStreamer : public YaRISCTargetStreamer {
+  formatted_raw_ostream &OS;
+
+public:
+  YaRISCTargetAsmStreamer(MCStreamer &S, formatted_raw_ostream &OS);
+};
+
+// This part is for ELF object output
+class YaRISCTargetELFStreamer : public YaRISCTargetStreamer {
+public:
+  YaRISCTargetELFStreamer(MCStreamer &S, const MCSubtargetInfo &STI);
+  MCELFStreamer &getStreamer();
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_MCTARGETDESC_YARISCTARGETSTREAMER_H
diff --git a/llvm/lib/Target/YaRISC/TargetInfo/CMakeLists.txt b/llvm/lib/Target/YaRISC/TargetInfo/CMakeLists.txt
new file mode 100644
index 000000000000..c13213c7ccfa
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/TargetInfo/CMakeLists.txt
@@ -0,0 +1,10 @@
+add_llvm_component_library(LLVMYaRISCInfo
+  YaRISCTargetInfo.cpp
+
+  LINK_COMPONENTS
+  MC
+  Support
+
+  ADD_TO_COMPONENT
+  YaRISC
+  )
diff --git a/llvm/lib/Target/YaRISC/TargetInfo/YaRISCTargetInfo.cpp b/llvm/lib/Target/YaRISC/TargetInfo/YaRISCTargetInfo.cpp
new file mode 100644
index 000000000000..c39a16f739d3
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/TargetInfo/YaRISCTargetInfo.cpp
@@ -0,0 +1,23 @@
+//===-- YaRISCTargetInfo.cpp - YaRISC Target Implementation ---------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "TargetInfo/YaRISCTargetInfo.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Compiler.h"
+using namespace llvm;
+
+Target &llvm::getTheYaRISCTarget() {
+  static Target TheYaRISCTarget;
+  return TheYaRISCTarget;
+}
+
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void
+LLVMInitializeYaRISCTargetInfo() {
+  RegisterTarget<Triple::yarisc, /*HasJIT=*/false> X(
+      getTheYaRISCTarget(), "yarisc", "Yet Another RISC", "YaRISC");
+}
diff --git a/llvm/lib/Target/YaRISC/TargetInfo/YaRISCTargetInfo.h b/llvm/lib/Target/YaRISC/TargetInfo/YaRISCTargetInfo.h
new file mode 100644
index 000000000000..2bad882e0e52
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/TargetInfo/YaRISCTargetInfo.h
@@ -0,0 +1,20 @@
+//===-- YaRISCTargetInfo.h - YaRISC Target Implementation -------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_TARGETINFO_YARISCTARGETINFO_H
+#define LLVM_LIB_TARGET_YARISC_TARGETINFO_YARISCTARGETINFO_H
+
+namespace llvm {
+
+class Target;
+
+Target &getTheYaRISCTarget();
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_TARGETINFO_YARISCTARGETINFO_H
diff --git a/llvm/lib/Target/YaRISC/YaRISC.h b/llvm/lib/Target/YaRISC/YaRISC.h
new file mode 100644
index 000000000000..2faae74720fc
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISC.h
@@ -0,0 +1,54 @@
+//===-- YaRISC.h - Interface for YaRISC representation ----------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains the entry points for global functions defined in the LLVM
+// YaRISC back-end.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_YARISC_H
+#define LLVM_LIB_TARGET_YARISC_YARISC_H
+
+#include "MCTargetDesc/YaRISCMCTargetDesc.h"
+#include "llvm/MC/MCInstrDesc.h"
+
+namespace llvm {
+class FunctionPass;
+class PassRegistry;
+class YaRISCTargetMachine;
+
+namespace YaRISCCC {
+// YaRISC specific condition code. These correspond to CC_* in
+// YaRISCInstrInfo.td. They must be kept in synch.
+enum CondCode : unsigned {
+  COND_EQ = 0b0001,
+  COND_LT = 0b0010,
+  COND_LE = 0b0011,
+  COND_LO = 0b0100,
+  COND_LS = 0b0101,
+  COND_NE = 0b1001,
+  COND_GE = 0b1010,
+  COND_GT = 0b1011,
+  COND_HS = 0b1100,
+  COND_HI = 0b1101,
+};
+} // namespace YaRISCCC
+
+namespace YaRISCOp {
+enum OperandType : unsigned {
+  OPERAND_COND_CODE = MCOI::OPERAND_FIRST_TARGET,
+};
+} // namespace YaRISCOp
+
+FunctionPass *createYaRISCISelDag(YaRISCTargetMachine &TM);
+
+void initializeYaRISCAsmPrinterPass(PassRegistry &);
+void initializeYaRISCDAGToDAGISelLegacyPass(PassRegistry &);
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_YARISC_H
diff --git a/llvm/lib/Target/YaRISC/YaRISC.td b/llvm/lib/Target/YaRISC/YaRISC.td
new file mode 100644
index 000000000000..6c62011633ee
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISC.td
@@ -0,0 +1,79 @@
+//===-- YaRISC.td - Describe the YaRISC Target Machine -----*- tablegen -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+//
+//===----------------------------------------------------------------------===//
+
+//===----------------------------------------------------------------------===//
+// Target-independent interfaces which we are implementing
+//===----------------------------------------------------------------------===//
+
+include "llvm/Target/Target.td"
+include "llvm/TableGen/SearchableTable.td"
+
+//===----------------------------------------------------------------------===//
+// YaRISC Subtarget features.
+//===----------------------------------------------------------------------===//
+
+def FeatureSoftFloat : SubtargetFeature<"soft-float", "UseSoftFloat", "true",
+                                   "Use software emulation for floating point">;
+
+def FeatureV1 : SubtargetFeature<"v1", "IsV1", "true",
+                                 "Enable YaRISC-V1 instructions">;
+
+//===----------------------------------------------------------------------===//
+// Register File, Calling Conv, Instruction Descriptions
+//===----------------------------------------------------------------------===//
+
+include "YaRISCRegisterInfo.td"
+include "YaRISCCallingConv.td"
+include "YaRISCInstrInfo.td"
+
+defm : RemapAllTargetPseudoPointerOperands<yarisc_ptr_rc>;
+
+def YaRISCInstrInfo : InstrInfo;
+
+def YaRISCAsmParser : AsmParser {
+  let ShouldEmitMatchRegisterAltName = true;
+  let AllowDuplicateRegisterNames = true;
+}
+
+def YaRISCAsmParserVariant : AsmParserVariant {
+  let RegisterPrefix = "%";
+}
+
+//===----------------------------------------------------------------------===//
+// YaRISC processors supported
+//===----------------------------------------------------------------------===//
+
+class Proc<string Name, list<SubtargetFeature> Features,
+           list<SubtargetFeature> TuneFeatures = []>
+ : Processor<Name, NoItineraries, Features, TuneFeatures>;
+
+def : Proc<"generic", [FeatureSoftFloat]>;
+def : Proc<"minimal", [FeatureSoftFloat]>;
+def : Proc<"v1",      [FeatureSoftFloat, FeatureV1]>;
+
+//===----------------------------------------------------------------------===//
+// Declare the target which we are implementing
+//===----------------------------------------------------------------------===//
+
+def YaRISCAsmWriter : AsmWriter {
+  string AsmWriterClassName  = "InstPrinter";
+  int PassSubtarget = 1;
+  int Variant = 0;
+}
+
+def YaRISC : Target {
+  // Pull in Instruction Info:
+  let InstructionSet = YaRISCInstrInfo;
+  let AssemblyParsers  = [YaRISCAsmParser];
+  let AssemblyParserVariants = [YaRISCAsmParserVariant];
+  let AssemblyWriters = [YaRISCAsmWriter];
+  let AllowRegisterRenaming = 1;
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCAsmPrinter.cpp b/llvm/lib/Target/YaRISC/YaRISCAsmPrinter.cpp
new file mode 100644
index 000000000000..f5e8daae6677
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCAsmPrinter.cpp
@@ -0,0 +1,177 @@
+//===-- YaRISCAsmPrinter.cpp - YaRISC LLVM assembly writer ----------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains a printer that converts from the internal representation
+// of machine-dependent LLVM code to assembly language.
+//
+//===----------------------------------------------------------------------===//
+
+#include "MCTargetDesc/YaRISCInstPrinter.h"
+#include "MCTargetDesc/YaRISCMCAsmInfo.h"
+#include "MCTargetDesc/YaRISCMCTargetDesc.h"
+#include "MCTargetDesc/YaRISCTargetStreamer.h"
+#include "TargetInfo/YaRISCTargetInfo.h"
+#include "YaRISC.h"
+#include "YaRISCInstrInfo.h"
+#include "YaRISCTargetMachine.h"
+#include "llvm/BinaryFormat/ELF.h"
+#include "llvm/CodeGen/AsmPrinter.h"
+#include "llvm/CodeGen/MachineInstr.h"
+#include "llvm/CodeGen/MachineModuleInfoImpls.h"
+#include "llvm/CodeGen/MachineRegisterInfo.h"
+#include "llvm/CodeGen/TargetLoweringObjectFileImpl.h"
+#include "llvm/IR/Mangler.h"
+#include "llvm/MC/MCAsmInfo.h"
+#include "llvm/MC/MCContext.h"
+#include "llvm/MC/MCInst.h"
+#include "llvm/MC/MCStreamer.h"
+#include "llvm/MC/MCSymbol.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Compiler.h"
+#include "llvm/Support/raw_ostream.h"
+
+using namespace llvm;
+
+#define DEBUG_TYPE "yarisc-asm-printer"
+
+namespace {
+class YaRISCAsmPrinter : public AsmPrinter {
+public:
+  static char ID;
+
+  explicit YaRISCAsmPrinter(TargetMachine &TM,
+                            std::unique_ptr<MCStreamer> Streamer)
+      : AsmPrinter(TM, std::move(Streamer), ID) {}
+
+  StringRef getPassName() const override { return "YaRISC Assembly Printer"; }
+
+  void emitInstruction(const MachineInstr *MI) override;
+
+  void lowerInstruction(const MachineInstr *MI, MCInst &OutMI) const;
+  bool lowerOperand(const MachineOperand &MO, MCOperand &MCOp) const;
+
+private:
+  MCOperand lowerSymbolOperand(const MachineOperand &MO, MCSymbol *Sym) const;
+};
+} // namespace
+
+void YaRISCAsmPrinter::emitInstruction(const MachineInstr *MI) {
+  if (MI->isBundle()) {
+    const MachineBasicBlock *MBB = MI->getParent();
+    MachineBasicBlock::const_instr_iterator I = ++MI->getIterator();
+    while (I != MBB->instr_end() && I->isInsideBundle()) {
+      emitInstruction(&*I);
+      ++I;
+    }
+    return;
+  }
+
+  if (MI->isPseudo())
+    return;
+
+  MCInst OutInst;
+  lowerInstruction(MI, OutInst);
+  EmitToStreamer(*OutStreamer, OutInst);
+}
+
+void YaRISCAsmPrinter::lowerInstruction(const MachineInstr *MI,
+                                        MCInst &OutMI) const {
+  OutMI.setOpcode(MI->getOpcode());
+
+  for (const MachineOperand &MO : MI->operands()) {
+    MCOperand MCOp;
+    if (lowerOperand(MO, MCOp))
+      OutMI.addOperand(MCOp);
+  }
+}
+
+bool YaRISCAsmPrinter::lowerOperand(const MachineOperand &MO,
+                                    MCOperand &MCOp) const {
+  switch (MO.getType()) {
+  default:
+    report_fatal_error("lowerOperand: unknown operand type");
+  case MachineOperand::MO_Register:
+    if (MO.isImplicit())
+      return false;
+    MCOp = MCOperand::createReg(MO.getReg());
+    break;
+  case MachineOperand::MO_Immediate:
+    MCOp = MCOperand::createImm(MO.getImm());
+    break;
+  case MachineOperand::MO_MachineBasicBlock:
+    MCOp = lowerSymbolOperand(MO, MO.getMBB()->getSymbol());
+    break;
+  case MachineOperand::MO_GlobalAddress:
+    MCOp = lowerSymbolOperand(MO, getSymbolPreferLocal(*MO.getGlobal()));
+    break;
+  case MachineOperand::MO_BlockAddress:
+    MCOp = lowerSymbolOperand(MO, GetBlockAddressSymbol(MO.getBlockAddress()));
+    break;
+  case MachineOperand::MO_ExternalSymbol:
+    MCOp = lowerSymbolOperand(MO, GetExternalSymbolSymbol(MO.getSymbolName()));
+    break;
+  case MachineOperand::MO_ConstantPoolIndex:
+    MCOp = lowerSymbolOperand(MO, GetCPISymbol(MO.getIndex()));
+    break;
+  case MachineOperand::MO_JumpTableIndex:
+    MCOp = lowerSymbolOperand(MO, GetJTISymbol(MO.getIndex()));
+    break;
+  case MachineOperand::MO_MCSymbol:
+    MCOp = lowerSymbolOperand(MO, MO.getMCSymbol());
+    break;
+  }
+  return true;
+}
+
+MCOperand YaRISCAsmPrinter::lowerSymbolOperand(const MachineOperand &MO,
+                                               MCSymbol *Sym) const {
+  uint16_t Spec = 0;
+  unsigned TF = MO.getTargetFlags();
+  switch (TF) {
+  case YaRISCII::MO_None:
+    break;
+  case YaRISCII::MO_CALL:
+  case YaRISCII::MO_CALL_PLT:
+    assert(TF != YaRISCII::MO_CALL_PLT && "not yet implemented");
+    Spec = ELF::R_YARISC_16;
+    break;
+  case YaRISCII::MO_PCREL16:
+    Spec = ELF::R_YARISC_PCREL16;
+    break;
+  case YaRISCII::MO_PCREL5_S1:
+    Spec = ELF::R_YARISC_PCREL5_S1;
+    break;
+  case YaRISCII::MO_PCREL9_S1:
+    Spec = ELF::R_YARISC_PCREL9_S1;
+    break;
+  default:
+    llvm_unreachable("unknown target flag");
+  }
+
+  const MCExpr *ME = MCSymbolRefExpr::create(Sym, OutContext);
+
+  if (!MO.isJTI() && !MO.isMBB() && MO.getOffset()) {
+    ME = MCBinaryExpr::createAdd(
+        ME, MCConstantExpr::create(MO.getOffset(), OutContext), OutContext);
+  }
+
+  if (Spec != 0)
+    ME = MCSpecifierExpr::create(ME, Spec, OutContext);
+  return MCOperand::createExpr(ME);
+}
+
+char YaRISCAsmPrinter::ID = 0;
+
+INITIALIZE_PASS(YaRISCAsmPrinter, "yarisc-asm-printer",
+                "YaRISC Assembly Printer", false, false)
+
+// Force static initialization.
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void
+LLVMInitializeYaRISCAsmPrinter() {
+  RegisterAsmPrinter<YaRISCAsmPrinter> X(getTheYaRISCTarget());
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCCallingConv.td b/llvm/lib/Target/YaRISC/YaRISCCallingConv.td
new file mode 100644
index 000000000000..f382ccb7efcd
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCCallingConv.td
@@ -0,0 +1,29 @@
+//===-- YaRISCCallingConv.td - Calling Conventions YaRISC --*- tablegen -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This describes the calling conventions for the YaRISC architecture.
+//
+//===----------------------------------------------------------------------===//
+
+def CC_YaRISC : CallingConv<[
+  // TODO: YaRISC
+  CCIfType<[i1, i8], CCPromoteToType<i16>>,
+  CCIfType<[i16], CCAssignToReg<[R0, R1]>>,
+  CCIfType<[i32], CCAssignToReg<[R0, R1]>>,
+  // Alternatively, they are assigned to the stack in 2-byte aligned units.
+  CCAssignToStack<2, 2>
+]>;
+
+def RetCC_YaRISC : CallingConv<[
+  // TODO: YaRISC
+  CCIfType<[i1, i8], CCPromoteToType<i16>>,
+  CCIfType<[i16], CCAssignToReg<[R0, R1]>>,
+  CCIfType<[i32], CCAssignToReg<[R0, R1]>>
+]>;
+
+def CSR : CalleeSavedRegs<(add R3, R4, R5)>;
diff --git a/llvm/lib/Target/YaRISC/YaRISCFrameLowering.cpp b/llvm/lib/Target/YaRISC/YaRISCFrameLowering.cpp
new file mode 100644
index 000000000000..255baa4e4706
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCFrameLowering.cpp
@@ -0,0 +1,98 @@
+//===-- YaRISCFrameLowering.cpp - YaRISC Frame Information ----------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains the YaRISC implementation of TargetFrameLowering class.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCFrameLowering.h"
+#include "YaRISCInstrInfo.h"
+#include "YaRISCSubtarget.h"
+#include "llvm/CodeGen/MachineFrameInfo.h"
+#include "llvm/CodeGen/MachineFunction.h"
+#include "llvm/CodeGen/MachineInstrBuilder.h"
+#include "llvm/Support/MathExtras.h"
+#include "llvm/Target/TargetMachine.h"
+#include "llvm/Target/TargetOptions.h"
+
+using namespace llvm;
+
+YaRISCFrameLowering::YaRISCFrameLowering(const YaRISCSubtarget &ST)
+    : TargetFrameLowering(TargetFrameLowering::StackGrowsDown, Align(2), 0) {}
+
+static void adjustSP(MachineBasicBlock &MBB, MachineBasicBlock::iterator MBBI,
+                     const DebugLoc &DL, const TargetInstrInfo &TII,
+                     int64_t Adjustment) {
+  if (Adjustment != 0) {
+    bool IsSmallImm = isInt<4>(Adjustment);
+    unsigned Opcode = IsSmallImm ? YaRISC::ADDri4 : YaRISC::ADDri16;
+
+    BuildMI(MBB, MBBI, DL, TII.get(Opcode), YaRISC::SP)
+        .addReg(YaRISC::SP)
+        .addImm(Adjustment);
+  }
+}
+
+void YaRISCFrameLowering::emitPrologue(MachineFunction &MF,
+                                       MachineBasicBlock &MBB) const {
+  MachineBasicBlock::iterator MBBI = MBB.begin();
+  DebugLoc DL = (MBBI != MBB.end()) ? MBBI->getDebugLoc() : DebugLoc();
+  const TargetInstrInfo &TII = *MF.getSubtarget().getInstrInfo();
+  uint64_t StackSize = MF.getFrameInfo().getStackSize();
+
+  adjustSP(MBB, MBBI, DL, TII, -static_cast<int64_t>(StackSize));
+
+  if (hasFP(MF)) {
+    // Set new frame pointer.
+    BuildMI(MBB, MBBI, DL, TII.get(YaRISC::MOVr), YaRISC::R5)
+        .addReg(YaRISC::SP);
+  }
+}
+
+void YaRISCFrameLowering::emitEpilogue(MachineFunction &MF,
+                                       MachineBasicBlock &MBB) const {
+  MachineBasicBlock::iterator MBBI = MBB.getFirstTerminator();
+  DebugLoc DL = (MBBI != MBB.end()) ? MBBI->getDebugLoc() : DebugLoc();
+  const TargetInstrInfo &TII = *MF.getSubtarget().getInstrInfo();
+  uint64_t StackSize = MF.getFrameInfo().getStackSize();
+
+  adjustSP(MBB, MBBI, DL, TII, static_cast<int64_t>(StackSize));
+}
+
+MachineBasicBlock::iterator YaRISCFrameLowering::eliminateCallFramePseudoInstr(
+    MachineFunction &MF, MachineBasicBlock &MBB,
+    MachineBasicBlock::iterator I) const {
+  const TargetInstrInfo &TII = *MF.getSubtarget().getInstrInfo();
+  DebugLoc DL = I->getDebugLoc();
+  int64_t Amount = I->getOperand(0).getImm();
+
+  if (Amount != 0 && !hasReservedCallFrame(MF)) {
+    assert(isInt<16>(Amount) && "Offset too large");
+    assert((I->getOpcode() == TII.getCallFrameSetupOpcode() ||
+            I->getOpcode() == TII.getCallFrameDestroyOpcode()) &&
+           "Unknown frame pseudo instruction");
+
+    if (I->getOpcode() == TII.getCallFrameSetupOpcode())
+      Amount = -Amount;
+
+    bool IsSmallImm = isInt<4>(Amount);
+    unsigned Opcode = IsSmallImm ? YaRISC::ADDri4 : YaRISC::ADDri16;
+
+    BuildMI(MBB, I, DL, TII.get(Opcode), YaRISC::SP)
+        .addReg(YaRISC::SP)
+        .addImm(Amount);
+  }
+  return MBB.erase(I);
+}
+
+bool YaRISCFrameLowering::hasFPImpl(const MachineFunction &MF) const {
+  return MF.getTarget().Options.DisableFramePointerElim(MF) ||
+         MF.getFrameInfo().hasVarSizedObjects() ||
+         MF.getFrameInfo().isFrameAddressTaken() ||
+         MF.getSubtarget().getRegisterInfo()->hasStackRealignment(MF);
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCFrameLowering.h b/llvm/lib/Target/YaRISC/YaRISCFrameLowering.h
new file mode 100644
index 000000000000..236571379fd1
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCFrameLowering.h
@@ -0,0 +1,42 @@
+//===-- YaRISCFrameLowering.h - Frame lowering for YaRISC -------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains the YaRISC definition of the TargetFrameLowering class.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_YARISCFRAMELOWERING_H
+#define LLVM_LIB_TARGET_YARISC_YARISCFRAMELOWERING_H
+
+#include "YaRISC.h"
+#include "llvm/CodeGen/TargetFrameLowering.h"
+#include "llvm/Support/TypeSize.h"
+
+namespace llvm {
+
+class YaRISCSubtarget;
+class YaRISCFrameLowering : public TargetFrameLowering {
+public:
+  explicit YaRISCFrameLowering(const YaRISCSubtarget &ST);
+
+  /// emitProlog/emitEpilog - These methods insert prolog and epilog code into
+  /// the function.
+  void emitPrologue(MachineFunction &MF, MachineBasicBlock &MBB) const override;
+  void emitEpilogue(MachineFunction &MF, MachineBasicBlock &MBB) const override;
+
+  MachineBasicBlock::iterator
+  eliminateCallFramePseudoInstr(MachineFunction &MF, MachineBasicBlock &MBB,
+                                MachineBasicBlock::iterator I) const override;
+
+protected:
+  bool hasFPImpl(const MachineFunction &MF) const override;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_YARISCFRAMELOWERING_H
diff --git a/llvm/lib/Target/YaRISC/YaRISCISelDAGToDAG.cpp b/llvm/lib/Target/YaRISC/YaRISCISelDAGToDAG.cpp
new file mode 100644
index 000000000000..1308746d1125
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCISelDAGToDAG.cpp
@@ -0,0 +1,70 @@
+//===-- YaRISCISelDAGToDAG.cpp - A dag to dag inst selector for YaRISC ----===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file defines an instruction selector for the YaRISC target.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCSelectionDAGInfo.h"
+#include "YaRISCTargetMachine.h"
+#include "llvm/CodeGen/MachineRegisterInfo.h"
+#include "llvm/CodeGen/SelectionDAGISel.h"
+#include "llvm/Support/ErrorHandling.h"
+
+using namespace llvm;
+
+#define DEBUG_TYPE "yarisc-isel"
+#define PASS_NAME "YaRISC DAG->DAG Pattern Instruction Selection"
+
+//===----------------------------------------------------------------------===//
+// Instruction Selector Implementation
+//===----------------------------------------------------------------------===//
+
+//===--------------------------------------------------------------------===//
+/// YaRISCDAGToDAGISel - YaRISC specific code to select machine instructions for
+/// SelectionDAG operations.
+///
+namespace {
+class YaRISCDAGToDAGISel final : public SelectionDAGISel {
+public:
+  explicit YaRISCDAGToDAGISel(YaRISCTargetMachine &TM) : SelectionDAGISel(TM) {}
+
+  void Select(SDNode *N) override;
+
+  // Include the pieces autogenerated from the target description.
+#include "YaRISCGenDAGISel.inc"
+};
+
+class YaRISCDAGToDAGISelLegacy final : public SelectionDAGISelLegacy {
+public:
+  static char ID;
+
+  explicit YaRISCDAGToDAGISelLegacy(YaRISCTargetMachine &TM)
+      : SelectionDAGISelLegacy(ID, std::make_unique<YaRISCDAGToDAGISel>(TM)) {}
+};
+} // namespace
+
+char YaRISCDAGToDAGISelLegacy::ID = 0;
+
+INITIALIZE_PASS(YaRISCDAGToDAGISelLegacy, DEBUG_TYPE, PASS_NAME, false, false)
+
+void YaRISCDAGToDAGISel::Select(SDNode *N) {
+  if (N->isMachineOpcode()) {
+    N->setNodeId(-1);
+    return; // Already selected.
+  }
+
+  SelectCode(N);
+}
+
+/// createYaRISCISelDag - This pass converts a legalized DAG into a
+/// YaRISC-specific DAG, ready for instruction scheduling.
+///
+FunctionPass *llvm::createYaRISCISelDag(YaRISCTargetMachine &TM) {
+  return new YaRISCDAGToDAGISelLegacy(TM);
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCISelLowering.cpp b/llvm/lib/Target/YaRISC/YaRISCISelLowering.cpp
new file mode 100644
index 000000000000..60a021835728
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCISelLowering.cpp
@@ -0,0 +1,519 @@
+//===-- YaRISCISelLowering.cpp - YaRISC DAG Lowering Implementation -------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file implements the interfaces that YaRISC uses to lower LLVM code into
+// a selection DAG.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCISelLowering.h"
+#include "MCTargetDesc/YaRISCMCTargetDesc.h"
+#include "YaRISCRegisterInfo.h"
+#include "YaRISCSelectionDAGInfo.h"
+#include "YaRISCTargetMachine.h"
+#include "llvm/ADT/StringExtras.h"
+#include "llvm/ADT/StringSwitch.h"
+#include "llvm/BinaryFormat/ELF.h"
+#include "llvm/CodeGen/CallingConvLower.h"
+#include "llvm/CodeGen/MachineFrameInfo.h"
+#include "llvm/CodeGen/MachineFunction.h"
+#include "llvm/CodeGen/MachineInstrBuilder.h"
+#include "llvm/CodeGen/MachineRegisterInfo.h"
+#include "llvm/CodeGen/SelectionDAG.h"
+#include "llvm/CodeGen/SelectionDAGNodes.h"
+#include "llvm/CodeGen/TargetLowering.h"
+#include "llvm/CodeGen/TargetLoweringObjectFileImpl.h"
+#include "llvm/IR/DerivedTypes.h"
+#include "llvm/IR/DiagnosticInfo.h"
+#include "llvm/IR/Function.h"
+#include "llvm/IR/IRBuilder.h"
+#include "llvm/IR/Module.h"
+#include "llvm/Support/ErrorHandling.h"
+#include "llvm/Support/KnownBits.h"
+
+using namespace llvm;
+
+//===----------------------------------------------------------------------===//
+// Calling Convention Implementation
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCGenCallingConv.inc"
+
+// TODO: YaRISC Anything special to do for isByVal()?
+
+SDValue YaRISCTargetLowering::LowerFormalArguments(
+    SDValue Chain, CallingConv::ID CallConv, bool IsVarArg,
+    const SmallVectorImpl<ISD::InputArg> &Ins, const SDLoc &DL,
+    SelectionDAG &DAG, SmallVectorImpl<SDValue> &InVals) const {
+  MachineFunction &MF = DAG.getMachineFunction();
+  MachineFrameInfo &MFI = MF.getFrameInfo();
+
+  SmallVector<CCValAssign, 16> ArgLocs;
+  CCState CCInfo(CallConv, IsVarArg, MF, ArgLocs, *DAG.getContext());
+
+  CCInfo.AnalyzeFormalArguments(Ins, CC_YaRISC);
+
+  for (const CCValAssign &VA : ArgLocs) {
+    if (VA.isRegLoc()) {
+      Register VReg = MF.addLiveIn(VA.getLocReg(), &YaRISC::GPRRegClass);
+      SDValue Val = DAG.getCopyFromReg(Chain, DL, VReg, VA.getLocVT());
+
+      switch (VA.getLocInfo()) {
+      default:
+        llvm_unreachable("Unknown loc info!");
+      case CCValAssign::Full:
+        break;
+      case CCValAssign::SExt:
+        Val = DAG.getSExtOrTrunc(Val, DL, VA.getValVT());
+        break;
+      case CCValAssign::ZExt:
+        Val = DAG.getZExtOrTrunc(Val, DL, VA.getValVT());
+        break;
+      case CCValAssign::AExt:
+        Val = DAG.getAnyExtOrTrunc(Val, DL, VA.getValVT());
+        break;
+      case CCValAssign::Trunc:
+        Val = DAG.getNode(ISD::TRUNCATE, DL, VA.getValVT(), Val);
+        break;
+      }
+
+      InVals.push_back(Val);
+    } else {
+      assert(VA.isMemLoc() && "Unknown arg location");
+
+      unsigned Size = VA.getValVT().getStoreSize();
+      int FI = MFI.CreateFixedObject(Size, VA.getLocMemOffset(), true);
+      SDValue FIN = DAG.getFrameIndex(FI, MVT::i16);
+
+      SDValue Load = DAG.getLoad(VA.getValVT(), DL, Chain, FIN,
+                                 MachinePointerInfo::getFixedStack(MF, FI),
+                                 Align(2)); // 2-byte stack alignment for 16-bit
+
+      InVals.push_back(Load);
+    }
+  }
+
+  return Chain;
+}
+
+SDValue
+YaRISCTargetLowering::LowerCall(TargetLowering::CallLoweringInfo &CLI,
+                                SmallVectorImpl<SDValue> &InVals) const {
+  SelectionDAG &DAG = CLI.DAG;
+  SDLoc &DL = CLI.DL;
+  const SmallVectorImpl<ISD::OutputArg> &Outs = CLI.Outs;
+  const SmallVectorImpl<SDValue> &OutVals = CLI.OutVals;
+  const SmallVectorImpl<ISD::InputArg> &Ins = CLI.Ins;
+  SDValue Chain = CLI.Chain;
+  SDValue Callee = CLI.Callee;
+  CallingConv::ID CallConv = CLI.CallConv;
+  bool IsVarArg = CLI.IsVarArg;
+
+  // No tail call optimization so far.
+  CLI.IsTailCall = false;
+
+  MachineFunction &MF = DAG.getMachineFunction();
+
+  SmallVector<CCValAssign, 16> ArgLocs;
+  CCState CCInfo(CallConv, IsVarArg, MF, ArgLocs, *DAG.getContext());
+  CCInfo.AnalyzeCallOperands(Outs, CC_YaRISC);
+
+  unsigned BytesToReserve = CCInfo.getStackSize();
+  Align StackAlign(2); // 2-byte alignment for 16-bit
+
+  Chain = DAG.getCALLSEQ_START(Chain, BytesToReserve, 0, DL);
+
+  SmallVector<std::pair<unsigned, SDValue>, 3> RegsToPass;
+  SmallVector<SDValue, 8> MemOpChains;
+
+  for (unsigned i = 0; i < ArgLocs.size(); ++i) {
+    CCValAssign &VA = ArgLocs[i];
+    SDValue Val = OutVals[i];
+
+    switch (VA.getLocInfo()) {
+    default:
+      llvm_unreachable("Unknown loc info!");
+    case CCValAssign::Full:
+      break;
+    case CCValAssign::SExt:
+      Val = DAG.getSExtOrTrunc(Val, DL, VA.getLocVT());
+      break;
+    case CCValAssign::ZExt:
+      Val = DAG.getZExtOrTrunc(Val, DL, VA.getLocVT());
+      break;
+    case CCValAssign::AExt:
+      Val = DAG.getAnyExtOrTrunc(Val, DL, VA.getLocVT());
+      break;
+    case CCValAssign::Trunc:
+      Val = DAG.getNode(ISD::TRUNCATE, DL, VA.getLocVT(), Val);
+      break;
+    }
+
+    if (VA.isRegLoc()) {
+      RegsToPass.emplace_back(VA.getLocReg(), Val);
+    } else {
+      assert(VA.isMemLoc() && "Unexpected call argument location");
+
+      SDValue SP = DAG.getRegister(YaRISC::SP, MVT::i16);
+      SDValue Off = DAG.getConstant(VA.getLocMemOffset(), DL, MVT::i16);
+      SDValue Ptr = DAG.getNode(ISD::ADD, DL, MVT::i16, SP, Off);
+
+      SDValue Store = DAG.getStore(
+          Chain, DL, Val, Ptr,
+          MachinePointerInfo::getStack(MF, VA.getLocMemOffset()), StackAlign);
+
+      MemOpChains.push_back(Store);
+    }
+  }
+
+  if (!MemOpChains.empty())
+    Chain = DAG.getNode(ISD::TokenFactor, DL, MVT::Other, MemOpChains);
+
+  SDValue Glue;
+  for (auto &RegArg : RegsToPass) {
+    Chain = DAG.getCopyToReg(Chain, DL, RegArg.first, RegArg.second, Glue);
+    Glue = Chain.getValue(1);
+  }
+
+  // Not sure if we ever need PLT calls. For now distinguish them here here. The
+  // addresses are replaced here so that they can be matched with
+  // tglobaladdr:$target and texternalsym:$target when replaced by out call
+  // pseudo-instruction.
+  if (auto *S = dyn_cast<GlobalAddressSDNode>(Callee)) {
+    const GlobalValue *GV = S->getGlobal();
+    unsigned OpFlags = getTargetMachine().shouldAssumeDSOLocal(GV)
+                           ? YaRISCII::MO_CALL
+                           : YaRISCII::MO_CALL_PLT;
+    Callee = DAG.getTargetGlobalAddress(
+        GV, DL, getPointerTy(DAG.getDataLayout()), 0, OpFlags);
+  } else if (auto *S = dyn_cast<ExternalSymbolSDNode>(Callee)) {
+    unsigned OpFlags = getTargetMachine().shouldAssumeDSOLocal(nullptr)
+                           ? YaRISCII::MO_CALL
+                           : YaRISCII::MO_CALL_PLT;
+    Callee = DAG.getTargetExternalSymbol(
+        S->getSymbol(), getPointerTy(DAG.getDataLayout()), OpFlags);
+  }
+
+  SmallVector<SDValue, 8> Ops;
+  Ops.push_back(Chain);
+  Ops.push_back(Callee);
+
+  for (auto &Reg : RegsToPass)
+    Ops.push_back(DAG.getRegister(Reg.first, Reg.second.getValueType()));
+
+  const TargetRegisterInfo *TRI = Subtarget->getRegisterInfo();
+  const uint32_t *Mask =
+      TRI->getCallPreservedMask(DAG.getMachineFunction(), CallConv);
+  assert(Mask && "Missing call preserved mask for calling convention");
+  Ops.push_back(DAG.getRegisterMask(Mask));
+
+  if (Glue.getNode()) {
+    Ops.push_back(Glue);
+  }
+
+  SDVTList NodeTys = DAG.getVTList(MVT::Other, MVT::Glue);
+  Chain = DAG.getNode(YaRISCISD::CALL, DL, NodeTys, Ops);
+  Glue = Chain.getValue(1);
+
+  Chain = DAG.getCALLSEQ_END(Chain, BytesToReserve, 0, Glue, DL);
+
+  SmallVector<CCValAssign, 16> RetLocs;
+  CCState RetCCInfo(CallConv, IsVarArg, MF, RetLocs, *DAG.getContext());
+  RetCCInfo.AnalyzeCallResult(Ins, RetCC_YaRISC);
+
+  for (const CCValAssign &VA : RetLocs) {
+    assert(VA.isRegLoc() && "YaRISC returns only in registers");
+    SDValue Val = DAG.getCopyFromReg(Chain, DL, VA.getLocReg(), VA.getLocVT());
+
+    switch (VA.getLocInfo()) {
+    default:
+      llvm_unreachable("Unknown loc info!");
+    case CCValAssign::Full:
+      break;
+    case CCValAssign::SExt:
+      Val = DAG.getSExtOrTrunc(Val, DL, VA.getValVT());
+      break;
+    case CCValAssign::ZExt:
+      Val = DAG.getZExtOrTrunc(Val, DL, VA.getValVT());
+      break;
+    case CCValAssign::AExt:
+      Val = DAG.getAnyExtOrTrunc(Val, DL, VA.getValVT());
+      break;
+    case CCValAssign::Trunc:
+      Val = DAG.getNode(ISD::TRUNCATE, DL, VA.getValVT(), Val);
+      break;
+    }
+
+    InVals.push_back(Val);
+  }
+
+  return Chain;
+}
+
+SDValue
+YaRISCTargetLowering::LowerReturn(SDValue Chain, CallingConv::ID CallConv,
+                                  bool IsVarArg,
+                                  const SmallVectorImpl<ISD::OutputArg> &Outs,
+                                  const SmallVectorImpl<SDValue> &OutVals,
+                                  const SDLoc &DL, SelectionDAG &DAG) const {
+  MachineFunction &MF = DAG.getMachineFunction();
+
+  SmallVector<CCValAssign, 16> RVLocs;
+  CCState CCInfo(CallConv, IsVarArg, MF, RVLocs, *DAG.getContext());
+  CCInfo.AnalyzeReturn(Outs, RetCC_YaRISC);
+
+  SDValue Glue;
+  SmallVector<SDValue, 4> RetOps(1, Chain);
+
+  for (unsigned i = 0; i < RVLocs.size(); ++i) {
+    CCValAssign &VA = RVLocs[i];
+    SDValue Val = OutVals[i];
+
+    switch (VA.getLocInfo()) {
+    default:
+      llvm_unreachable("Unknown loc info!");
+    case CCValAssign::Full:
+      break;
+    case CCValAssign::SExt:
+      Val = DAG.getSExtOrTrunc(Val, DL, VA.getLocVT());
+      break;
+    case CCValAssign::ZExt:
+      Val = DAG.getZExtOrTrunc(Val, DL, VA.getLocVT());
+      break;
+    case CCValAssign::AExt:
+      Val = DAG.getAnyExtOrTrunc(Val, DL, VA.getLocVT());
+      break;
+    }
+
+    Register Reg = VA.getLocReg();
+    Chain = DAG.getCopyToReg(Chain, DL, Reg, Val, Glue);
+    Glue = Chain.getValue(1);
+    RetOps.push_back(DAG.getRegister(Reg, VA.getLocVT()));
+  }
+
+  if (MF.getFunction().hasStructRetAttr()) {
+    // TODO: YaRISC Anything special to do here?
+  }
+
+  // Update chain and glue.
+  RetOps[0] = Chain;
+  if (Glue.getNode())
+    RetOps.push_back(Glue);
+
+  return DAG.getNode(YaRISCISD::RET_GLUE, DL, MVT::Other, RetOps);
+}
+
+//===----------------------------------------------------------------------===//
+// TargetLowering Implementation
+//===----------------------------------------------------------------------===//
+
+YaRISCTargetLowering::YaRISCTargetLowering(const TargetMachine &TM,
+                                           const YaRISCSubtarget &STI)
+    : TargetLowering(TM, STI), Subtarget(&STI) {
+  setBooleanContents(ZeroOrOneBooleanContent);
+  setBooleanVectorContents(ZeroOrNegativeOneBooleanContent);
+
+  // Atomics are currently not supported.
+  setMaxAtomicSizeInBitsSupported(0);
+
+  setStackPointerRegisterToSaveRestore(YaRISC::SP);
+  setMinFunctionAlignment(Align(2));
+
+  addRegisterClass(MVT::i16, &YaRISC::GPRRegClass);
+  computeRegisterProperties(STI.getRegisterInfo());
+
+  setOperationAction(ISD::ADD, MVT::i8, Promote);
+  setOperationAction(ISD::ADD, MVT::i32, Expand);
+  setOperationAction(ISD::ADD, MVT::i64, Expand);
+  setOperationAction(ISD::SUB, MVT::i8, Promote);
+  setOperationAction(ISD::SUB, MVT::i32, Expand);
+  setOperationAction(ISD::SUB, MVT::i64, Expand);
+
+  setOperationAction(ISD::BR_CC, MVT::i8, Promote);
+  setOperationAction(ISD::BR_CC, MVT::i16, Custom);
+  setOperationAction(ISD::BR_CC, MVT::i32, Expand);
+  setOperationAction(ISD::BR_CC, MVT::i64, Expand);
+  setOperationAction(ISD::BRCOND, MVT::Other, Expand);
+
+  setOperationAction(ISD::SELECT, MVT::i8, Promote);
+  setOperationAction(ISD::SELECT, MVT::i16, Expand);
+  setOperationAction(ISD::SELECT, MVT::i32, Expand);
+  setOperationAction(ISD::SELECT, MVT::i64, Expand);
+  setOperationAction(ISD::SELECT_CC, MVT::i8, Promote);
+  setOperationAction(ISD::SELECT_CC, MVT::i16, Custom);
+  setOperationAction(ISD::SELECT_CC, MVT::i32, Expand);
+  setOperationAction(ISD::SELECT_CC, MVT::i64, Expand);
+  setOperationAction(ISD::SETCC, MVT::i8, Promote);
+  setOperationAction(ISD::SETCC, MVT::i16, Custom);
+  setOperationAction(ISD::SETCC, MVT::i32, Expand);
+  setOperationAction(ISD::SETCC, MVT::i64, Expand);
+}
+
+bool YaRISCTargetLowering::useSoftFloat() const {
+  return Subtarget->useSoftFloat();
+}
+
+SDValue YaRISCTargetLowering::LowerOperation(SDValue Op,
+                                             SelectionDAG &DAG) const {
+  switch (Op.getOpcode()) {
+  default:
+    llvm_unreachable("Should not custom lower this!");
+  case ISD::BR_CC:
+    return lowerBR_CC(Op, DAG);
+  case ISD::SELECT_CC:
+    return lowerSELECT_CC(Op, DAG);
+  case ISD::SETCC:
+    return lowerSETCC(Op, DAG);
+  }
+}
+
+MachineBasicBlock *YaRISCTargetLowering::EmitInstrWithCustomInserter(
+    MachineInstr &MI, MachineBasicBlock *MBB) const {
+  switch (MI.getOpcode()) {
+  default:
+    llvm_unreachable("Unexpected instr type to insert");
+  case YaRISC::SELECT:
+    return expandSelectCC(MI, MBB);
+  }
+}
+
+static YaRISCCC::CondCode intCCToYaRISCCC(ISD::CondCode CC) {
+  switch (CC) {
+  default:
+    llvm_unreachable("Unknown condition code!");
+  case ISD::SETEQ:
+    return YaRISCCC::COND_EQ;
+  case ISD::SETNE:
+    return YaRISCCC::COND_NE;
+  case ISD::SETLT:
+    return YaRISCCC::COND_LT;
+  case ISD::SETLE:
+    return YaRISCCC::COND_LE;
+  case ISD::SETGT:
+    return YaRISCCC::COND_GT;
+  case ISD::SETGE:
+    return YaRISCCC::COND_GE;
+  case ISD::SETULT:
+    return YaRISCCC::COND_LO;
+  case ISD::SETULE:
+    return YaRISCCC::COND_LS;
+  case ISD::SETUGT:
+    return YaRISCCC::COND_HI;
+  case ISD::SETUGE:
+    return YaRISCCC::COND_HS;
+  }
+}
+
+SDValue YaRISCTargetLowering::lowerBR_CC(SDValue Op, SelectionDAG &DAG) const {
+  SDLoc DL(Op);
+  SDValue Chain = Op.getOperand(0);
+  ISD::CondCode CC = cast<CondCodeSDNode>(Op.getOperand(1))->get();
+  SDValue LHS = Op.getOperand(2);
+  SDValue RHS = Op.getOperand(3);
+  SDValue Dest = Op.getOperand(4);
+
+  // Sets the status flags.
+  SDValue Cmp = DAG.getNode(YaRISCISD::CMP, DL, MVT::Glue, LHS, RHS);
+  SDValue CCVal = DAG.getConstant(intCCToYaRISCCC(CC), DL, MVT::i16);
+
+  return DAG.getNode(YaRISCISD::BRCOND, DL, MVT::Other, Chain, Dest, CCVal,
+                     Cmp);
+}
+
+SDValue YaRISCTargetLowering::lowerSELECT_CC(SDValue Op,
+                                             SelectionDAG &DAG) const {
+  SDLoc DL(Op);
+  SDValue LHS = Op.getOperand(0);
+  SDValue RHS = Op.getOperand(1);
+  SDValue TrueVal = Op.getOperand(2);
+  SDValue FalseVal = Op.getOperand(3);
+  ISD::CondCode CC = cast<CondCodeSDNode>(Op.getOperand(4))->get();
+
+  // Sets the status flags.
+  SDValue Cmp = DAG.getNode(YaRISCISD::CMP, DL, MVT::Glue, LHS, RHS);
+  SDValue CCVal = DAG.getConstant(intCCToYaRISCCC(CC), DL, MVT::i16);
+
+  return DAG.getNode(YaRISCISD::SELECT_CC, DL, TrueVal.getValueType(), TrueVal,
+                     FalseVal, CCVal, Cmp);
+}
+
+SDValue YaRISCTargetLowering::lowerSETCC(SDValue Op, SelectionDAG &DAG) const {
+  SDLoc DL(Op);
+  SDValue LHS = Op.getOperand(0);
+  SDValue RHS = Op.getOperand(1);
+  ISD::CondCode CC = cast<CondCodeSDNode>(Op.getOperand(2))->get();
+
+  // Sets the status flags.
+  SDValue Cmp = DAG.getNode(YaRISCISD::CMP, DL, MVT::Glue, LHS, RHS);
+  SDValue CCVal = DAG.getConstant(intCCToYaRISCCC(CC), DL, MVT::i16);
+
+  SDValue TrueVal = DAG.getConstant(1, DL, Op.getValueType());
+  SDValue FalseVal = DAG.getConstant(0, DL, Op.getValueType());
+
+  return DAG.getNode(YaRISCISD::SELECT_CC, DL, TrueVal.getValueType(), TrueVal,
+                     FalseVal, CCVal, Cmp);
+}
+
+MachineBasicBlock *
+YaRISCTargetLowering::expandSelectCC(MachineInstr &MI,
+                                     MachineBasicBlock *BB) const {
+  const TargetInstrInfo &TII = *Subtarget->getInstrInfo();
+  const DebugLoc &DL = MI.getDebugLoc();
+  unsigned CC = static_cast<YaRISCCC::CondCode>(MI.getOperand(3).getImm());
+
+  // To "insert" a SELECT_CC instruction, we actually have to insert the
+  // triangle control-flow pattern. The incoming instruction knows the
+  // destination vreg to set, the condition code register to branch on, the
+  // true/false values to select between, and the condition code for the branch.
+  //
+  // We produce the following control flow:
+  //     ThisMBB
+  //     |  \
+  //     |  IfFalseMBB
+  //     | /
+  //    SinkMBB
+  const BasicBlock *LLVM_BB = BB->getBasicBlock();
+  MachineFunction::iterator It = ++BB->getIterator();
+
+  MachineBasicBlock *ThisMBB = BB;
+  MachineFunction *F = BB->getParent();
+  MachineBasicBlock *IfFalseMBB = F->CreateMachineBasicBlock(LLVM_BB);
+  MachineBasicBlock *SinkMBB = F->CreateMachineBasicBlock(LLVM_BB);
+  F->insert(It, IfFalseMBB);
+  F->insert(It, SinkMBB);
+
+  // Transfer the remainder of ThisMBB and its successor edges to SinkMBB.
+  SinkMBB->splice(SinkMBB->begin(), ThisMBB,
+                  std::next(MachineBasicBlock::iterator(MI)), ThisMBB->end());
+  SinkMBB->transferSuccessorsAndUpdatePHIs(ThisMBB);
+
+  // Set the new successors for ThisMBB.
+  ThisMBB->addSuccessor(IfFalseMBB);
+  ThisMBB->addSuccessor(SinkMBB);
+
+  // TODO: YaRISC Use pseudo-instruction YaRISC::BCCa and expand in branch
+  //              relaxation to either YaRISC::BCCa5 or YaRISC::BCCa16.
+  BuildMI(ThisMBB, DL, TII.get(YaRISC::BCCa16))
+      .addMBB(SinkMBB, YaRISCII::MO_PCREL16)
+      .addImm(CC);
+
+  // IfFalseMBB just falls through to SinkMBB.
+  IfFalseMBB->addSuccessor(SinkMBB);
+
+  // %Result = phi [ %TrueValue, ThisMBB ], [ %FalseValue, IfFalseMBB ]
+  BuildMI(*SinkMBB, SinkMBB->begin(), DL, TII.get(YaRISC::PHI),
+          MI.getOperand(0).getReg())
+      .addReg(MI.getOperand(1).getReg())
+      .addMBB(ThisMBB)
+      .addReg(MI.getOperand(2).getReg())
+      .addMBB(IfFalseMBB);
+
+  // The pseudo instruction is gone now.
+  MI.eraseFromParent();
+  return SinkMBB;
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCISelLowering.h b/llvm/lib/Target/YaRISC/YaRISCISelLowering.h
new file mode 100644
index 000000000000..364757ba1603
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCISelLowering.h
@@ -0,0 +1,62 @@
+//===-- YaRISCISelLowering.h - YaRISC DAG Lowering Interface ----*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file defines the interfaces that YaRISC uses to lower LLVM code into a
+// selection DAG.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_YARISCISELLOWERING_H
+#define LLVM_LIB_TARGET_YARISC_YARISCISELLOWERING_H
+
+#include "YaRISC.h"
+#include "llvm/CodeGen/TargetLowering.h"
+
+namespace llvm {
+class YaRISCSubtarget;
+
+class YaRISCTargetLowering : public TargetLowering {
+  const YaRISCSubtarget *Subtarget;
+
+public:
+  YaRISCTargetLowering(const TargetMachine &TM, const YaRISCSubtarget &STI);
+
+  bool useSoftFloat() const override;
+
+  SDValue LowerFormalArguments(SDValue Chain, CallingConv::ID CallConv,
+                               bool IsVarArg,
+                               const SmallVectorImpl<ISD::InputArg> &Ins,
+                               const SDLoc &DL, SelectionDAG &DAG,
+                               SmallVectorImpl<SDValue> &InVals) const override;
+
+  SDValue LowerCall(TargetLowering::CallLoweringInfo &CLI,
+                    SmallVectorImpl<SDValue> &InVals) const override;
+
+  SDValue LowerReturn(SDValue Chain, CallingConv::ID CallConv, bool IsVarArg,
+                      const SmallVectorImpl<ISD::OutputArg> &Outs,
+                      const SmallVectorImpl<SDValue> &OutVals, const SDLoc &DL,
+                      SelectionDAG &DAG) const override;
+
+  SDValue LowerOperation(SDValue Op, SelectionDAG &DAG) const override;
+
+  MachineBasicBlock *
+  EmitInstrWithCustomInserter(MachineInstr &MI,
+                              MachineBasicBlock *MBB) const override;
+
+private:
+  SDValue lowerBR_CC(SDValue Op, SelectionDAG &DAG) const;
+  SDValue lowerSELECT_CC(SDValue Op, SelectionDAG &DAG) const;
+  SDValue lowerSETCC(SDValue Op, SelectionDAG &DAG) const;
+
+  MachineBasicBlock *expandSelectCC(MachineInstr &MI,
+                                    MachineBasicBlock *BB) const;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_YARISCISELLOWERING_H
diff --git a/llvm/lib/Target/YaRISC/YaRISCInstrFormats.td b/llvm/lib/Target/YaRISC/YaRISCInstrFormats.td
new file mode 100644
index 000000000000..4b1797a13b81
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCInstrFormats.td
@@ -0,0 +1,229 @@
+//===-- YaRISCInstrFormats.td - YaRISC Instruction Formats -*- tablegen -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+class InstYaRISC<int sz, dag outs, dag ins, string asmstr, list<dag> pattern,
+                 InstrItinClass itin = NoItinerary>
+    : Instruction {
+  field bits<32> Inst;
+
+  let Namespace = "YaRISC";
+  let Size = sz;
+
+  bits<6> opcode;
+  let Inst{5-0} = opcode;
+
+  dag OutOperandList = outs;
+  dag InOperandList = ins;
+  let AsmString = asmstr;
+  let Pattern = pattern;
+
+  let Itinerary = itin;
+}
+
+class InstOp<int sz, dag outs, dag ins, string asmstr, list<dag> pattern,
+             InstrItinClass itin = NoItinerary>
+  : InstYaRISC<sz, outs, ins, asmstr, pattern, itin> {
+  bits<3> op0;
+  bits<6> ops;
+  bits<1> sel;
+  let Inst{8-6} = op0;
+  let Inst{14-9} = ops;
+  let Inst{15} = sel;
+  let mayLoad = 0;
+  let mayStore = 0;
+}
+
+class InstOpImm<int sz, dag outs, dag ins, string asmstr, list<dag> pattern,
+                InstrItinClass itin = NoItinerary>
+  : InstOp<sz, outs, ins, asmstr, pattern, itin> {
+  bits<4> st;
+  bits<1> as;
+  bits<1> loc;
+  let ops{3-0} = st;
+  let ops{4} = as;
+  let ops{5} = loc;
+  let sel = 1;
+}
+
+class InstOpImm4<dag outs, dag ins, string asmstr, list<dag> pattern,
+                 InstrItinClass itin = NoItinerary>
+  : InstOpImm<2, outs, ins, asmstr, pattern, itin> {
+  bits<4> imm;
+  let st = imm;
+  let loc = 0;
+}
+
+class InstOpImm16<dag outs, dag ins, string asmstr, list<dag> pattern,
+                  InstrItinClass itin = NoItinerary>
+  : InstOpImm<4, outs, ins, asmstr, pattern, itin> {
+  bits<16> imm;
+  let Inst{31-16} = imm;
+  let loc = 1;
+}
+
+class InstOpReg<dag outs, dag ins, string asmstr, list<dag> pattern,
+                InstrItinClass itin = NoItinerary>
+  : InstOp<2, outs, ins, asmstr, pattern, itin> {
+  bits<3> op1;
+  bits<3> op2;
+  let ops{2-0} = op1;
+  let ops{5-3} = op2;
+  let sel = 0;
+}
+
+class InstOp0<bits<6> op, dag outs, dag ins, string asmstr, list<dag> pattern,
+              InstrItinClass itin = NoItinerary>
+  : InstOp<2, outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+  let op0 = 0;
+  let ops = 0;
+  let sel = 0;
+}
+
+class InstOp1Reg<bits<6> op, dag outs, dag ins, string asmstr,
+                 list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpReg<outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+  let op1 = 0;
+  let op2 = 0;
+}
+
+class InstOp2Reg<bits<6> op, dag outs, dag ins, string asmstr,
+                 list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpReg<outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+  let op2 = 0;
+}
+
+class InstOp2RegImm4<bits<6> op, dag outs, dag ins, string asmstr,
+                     list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpImm4<outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+  let as = 1;
+}
+
+class InstOp2RegImm16<bits<6> op, dag outs, dag ins, string asmstr,
+                      list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpImm16<outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+  let st = 0;
+  let as = 1;
+}
+
+class InstOp2Imm4Reg<bits<6> op, dag outs, dag ins, string asmstr,
+                     list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpImm4<outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+  let as = 0;
+}
+
+class InstOp2Imm16Reg<bits<6> op, dag outs, dag ins, string asmstr,
+                      list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpImm16<outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+  let st = 0;
+  let as = 0;
+}
+
+class InstOp3Reg<bits<6> op, dag outs, dag ins, string asmstr,
+                 list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpReg<outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+}
+
+class InstOp3RegImm4<bits<6> op, dag outs, dag ins, string asmstr,
+                     list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpImm4<outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+  let as = 1;
+}
+
+class InstOp3RegImm16<bits<6> op, dag outs, dag ins, string asmstr,
+                      list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpImm16<outs, ins, asmstr, pattern, itin> {
+  bits<3> op1;
+  let opcode = op;
+  let st{3} = 0;
+  let st{2-0} = op1;
+  let as = 1;
+}
+
+class InstOp3Imm4Reg<bits<6> op, dag outs, dag ins, string asmstr,
+                     list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpImm4<outs, ins, asmstr, pattern, itin> {
+  let opcode = op;
+  let as = 0;
+}
+
+class InstOp3Imm16Reg<bits<6> op, dag outs, dag ins, string asmstr,
+                      list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstOpImm16<outs, ins, asmstr, pattern, itin> {
+  bits<3> op2;
+  let opcode = op;
+  let st{3} = 0;
+  let st{2-0} = op2;
+  let as = 0;
+}
+
+class InstBranch<int sz, dag outs, dag ins, string asmstr, list<dag> pattern,
+                 InstrItinClass itin = NoItinerary>
+  : InstYaRISC<sz, outs, ins, asmstr, pattern, itin> {
+  let isBranch = 1;
+  let isTerminator = 1;
+  let isBarrier = 1;
+
+  bits<1> aloc;
+  let Inst{15} = aloc;
+}
+
+class InstBranchAddr9<bits<6> op, dag outs, dag ins, string asmstr,
+                      list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstBranch<2, outs, ins, asmstr, pattern, itin> {
+  bits<9> addr;
+  let Inst{14-6} = addr;
+  let opcode = op;
+  let aloc = 0;
+}
+
+class InstBranchAddr16<bits<6> op, dag outs, dag ins, string asmstr,
+                       list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstBranch<4, outs, ins, asmstr, pattern, itin> {
+  bits<16> addr;
+  let Inst{14-6} = 0;
+  let Inst{31-16} = addr;
+  let opcode = op;
+  let aloc = 1;
+}
+
+class InstBranchCond<int sz, dag outs, dag ins, string asmstr,
+                     list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstBranch<sz, outs, ins, asmstr, pattern, itin> {
+  let isBarrier = 0;
+
+  bits<4> cc;
+  let Inst{9-6} = cc;
+}
+
+class InstBranchCond5<bits<6> op, dag outs, dag ins, string asmstr,
+                      list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstBranchCond<2, outs, ins, asmstr, pattern, itin> {
+  bits<5> addr;
+  let Inst{14-10} = addr;
+  let opcode = op;
+  let aloc = 0;
+}
+
+class InstBranchCond16<bits<6> op, dag outs, dag ins, string asmstr,
+                       list<dag> pattern, InstrItinClass itin = NoItinerary>
+  : InstBranchCond<4, outs, ins, asmstr, pattern, itin> {
+  bits<16> addr;
+  let Inst{14-10} = 0;
+  let Inst{31-16} = addr;
+  let opcode = op;
+  let aloc = 1;
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCInstrInfo.cpp b/llvm/lib/Target/YaRISC/YaRISCInstrInfo.cpp
new file mode 100644
index 000000000000..acb52c20d3e7
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCInstrInfo.cpp
@@ -0,0 +1,407 @@
+//===-- YaRISCInstrInfo.cpp - YaRISC Instruction Information --------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains the YaRISC implementation of the TargetInstrInfo class.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCInstrInfo.h"
+#include "YaRISC.h"
+#include "YaRISCSubtarget.h"
+#include "llvm/ADT/SmallVector.h"
+#include "llvm/CodeGen/MachineFrameInfo.h"
+#include "llvm/CodeGen/MachineInstrBuilder.h"
+#include "llvm/CodeGen/MachineMemOperand.h"
+#include "llvm/CodeGen/MachineRegisterInfo.h"
+#include "llvm/Support/ErrorHandling.h"
+#include "llvm/Support/MathExtras.h"
+#include "llvm/Target/TargetMachine.h"
+
+using namespace llvm;
+
+#define GET_INSTRINFO_CTOR_DTOR
+#include "YaRISCGenInstrInfo.inc"
+
+// Pin the vtable to this file.
+void YaRISCInstrInfo::anchor() {}
+
+YaRISCInstrInfo::YaRISCInstrInfo(const YaRISCSubtarget &ST)
+    : YaRISCGenInstrInfo(ST, RI, YaRISC::ADJCALLSTACKDOWN,
+                         YaRISC::ADJCALLSTACKUP),
+      RI(ST), Subtarget(ST) {}
+
+unsigned YaRISCInstrInfo::getInstSizeInBytes(const MachineInstr &MI) const {
+  switch (MI.getOpcode()) {
+  default:
+    return MI.getDesc().getSize();
+  case TargetOpcode::INLINEASM:
+  case TargetOpcode::INLINEASM_BR: {
+    const MachineFunction *MF = MI.getParent()->getParent();
+    const char *AsmStr = MI.getOperand(0).getSymbolName();
+    return getInlineAsmLength(AsmStr, *MF->getTarget().getMCAsmInfo());
+  }
+  }
+}
+
+void YaRISCInstrInfo::copyPhysReg(MachineBasicBlock &MBB,
+                                  MachineBasicBlock::iterator MBBI,
+                                  const DebugLoc &DL, Register DestReg,
+                                  Register SrcReg, bool KillSrc,
+                                  bool RenamableDest, bool RenamableSrc) const {
+  BuildMI(MBB, MBBI, DL, get(YaRISC::MOVr), DestReg)
+      .addReg(SrcReg, getKillRegState(KillSrc));
+}
+
+void YaRISCInstrInfo::storeRegToStackSlot(
+    MachineBasicBlock &MBB, MachineBasicBlock::iterator MBBI, Register SrcReg,
+    bool KillSrc, int FrameIndex, const TargetRegisterClass *RC, Register VReg,
+    MachineInstr::MIFlag Flags) const {
+  MachineFunction &MF = *MBB.getParent();
+  MachineFrameInfo &MFI = MF.getFrameInfo();
+  DebugLoc DL = MBBI != MBB.end() ? MBBI->getDebugLoc() : DebugLoc();
+
+  MachineMemOperand *MMO = MF.getMachineMemOperand(
+      MachinePointerInfo::getFixedStack(MF, FrameIndex),
+      MachineMemOperand::MOStore, MFI.getObjectSize(FrameIndex),
+      MFI.getObjectAlign(FrameIndex));
+
+  BuildMI(MBB, MBBI, DL, get(YaRISC::STXri16))
+      .addReg(SrcReg, getKillRegState(KillSrc))
+      .addFrameIndex(FrameIndex)
+      .addImm(0)
+      .addMemOperand(MMO);
+}
+
+void YaRISCInstrInfo::loadRegFromStackSlot(MachineBasicBlock &MBB,
+                                           MachineBasicBlock::iterator MBBI,
+                                           Register DestReg, int FrameIndex,
+                                           const TargetRegisterClass *RC,
+                                           Register VReg,
+                                           MachineInstr::MIFlag Flags) const {
+  MachineFunction &MF = *MBB.getParent();
+  MachineFrameInfo &MFI = MF.getFrameInfo();
+  DebugLoc DL = MBBI != MBB.end() ? MBBI->getDebugLoc() : DebugLoc();
+
+  MachineMemOperand *MMO = MF.getMachineMemOperand(
+      MachinePointerInfo::getFixedStack(MF, FrameIndex),
+      MachineMemOperand::MOLoad, MFI.getObjectSize(FrameIndex),
+      MFI.getObjectAlign(FrameIndex));
+
+  BuildMI(MBB, MBBI, DL, get(YaRISC::LDXri16), DestReg)
+      .addFrameIndex(FrameIndex)
+      .addImm(0)
+      .addMemOperand(MMO);
+}
+
+bool YaRISCInstrInfo::expandPostRAPseudo(MachineInstr &MI) const {
+  MachineBasicBlock &MBB = *MI.getParent();
+  const DebugLoc &DL = MI.getDebugLoc();
+
+  switch (MI.getOpcode()) {
+  case YaRISC::CALL: {
+    MachineFunction &MF = *MBB.getParent();
+
+    // Make room on the stack for the return address.
+    BuildMI(MBB, MI, DL, get(YaRISC::ADDri4), YaRISC::SP)
+        .addReg(YaRISC::SP)
+        .addImm(-2);
+
+    // The following sequence must stay together because we hard code the offset
+    // until after the jump.
+    MIBundleBuilder Bundle(MBB, MI);
+
+    // The final two instructions occupy six bytes in total. R2 is marked as
+    // implicitly defined. ADDri4 only supports input and output register to be
+    // the same.
+    BuildMI(MBB, MI, DL, get(YaRISC::ADDri16), YaRISC::R2)
+        .addReg(YaRISC::IP)
+        .addImm(6);
+
+    // Alternative to the previous instruction which also requires four bytes
+    // just like the ADDri16 instruction:
+    //
+    // Bundle.append(BuildMI(MF, DL, get(YaRISC::MOVi4), YaRISC::R2).addImm(6));
+    // Bundle.append(BuildMI(MF, DL, get(YaRISC::ADDrr), YaRISC::R2)
+    //                   .addReg(YaRISC::R2)
+    //                   .addReg(YaRISC::IP));
+
+    // Store the calculated return address onto the stack.
+    Bundle.append(BuildMI(MF, DL, get(YaRISC::STRr))
+                      .addReg(YaRISC::R2)
+                      .addReg(YaRISC::SP));
+
+    // Position independent code is not supported.
+    Bundle.append(
+        BuildMI(MF, DL, get(YaRISC::MOVi16), YaRISC::IP).add(MI.getOperand(0)));
+
+    finalizeBundle(MBB, Bundle.begin(), Bundle.end());
+  } break;
+  case YaRISC::RET: {
+    // Update the stack pointer to pop the return address.
+    BuildMI(MBB, MI, DL, get(YaRISC::ADDri4), YaRISC::SP)
+        .addReg(YaRISC::SP)
+        .addImm(2);
+
+    // Load the return address from the stack into the instruction pointer.
+    BuildMI(MBB, MI, DL, get(YaRISC::LDXspi4), YaRISC::IP).addImm(-2);
+  } break;
+  default:
+    return false;
+  }
+
+  MI.eraseFromParent();
+  return true;
+}
+
+static void parseCondBranch(MachineInstr &LastInst, MachineBasicBlock *&Target,
+                            SmallVectorImpl<MachineOperand> &Cond) {
+  // Block ends with fall-through condbranch.
+  assert(LastInst.getDesc().isConditionalBranch() &&
+         "Unknown conditional branch");
+
+  unsigned Opc = LastInst.getOpcode();
+  int64_t CC = LastInst.getOperand(1).getImm();
+
+  Cond.push_back(MachineOperand::CreateImm(Opc));
+  Cond.push_back(MachineOperand::CreateImm(CC));
+  Target = LastInst.getOperand(0).getMBB();
+}
+
+static YaRISCCC::CondCode getInvertedCondCode(YaRISCCC::CondCode CC) {
+  switch (CC) {
+  default:
+    llvm_unreachable("Invalid condition!");
+  case YaRISCCC::COND_EQ:
+    return YaRISCCC::COND_NE;
+  case YaRISCCC::COND_LT:
+    return YaRISCCC::COND_GE;
+  case YaRISCCC::COND_LE:
+    return YaRISCCC::COND_GT;
+  case YaRISCCC::COND_LO:
+    return YaRISCCC::COND_HS;
+  case YaRISCCC::COND_LS:
+    return YaRISCCC::COND_HI;
+  case YaRISCCC::COND_NE:
+    return YaRISCCC::COND_EQ;
+  case YaRISCCC::COND_GE:
+    return YaRISCCC::COND_LT;
+  case YaRISCCC::COND_GT:
+    return YaRISCCC::COND_LE;
+  case YaRISCCC::COND_HS:
+    return YaRISCCC::COND_LO;
+  case YaRISCCC::COND_HI:
+    return YaRISCCC::COND_LS;
+  }
+}
+
+bool YaRISCInstrInfo::isBranchOffsetInRange(unsigned BranchOp,
+                                            int64_t BrOffset) const {
+
+  switch (BranchOp) {
+  default:
+    llvm_unreachable("unexpected opcode!");
+  case YaRISC::BCCa5:
+    // The branch address is relative to the next instruction.
+    BrOffset -= 2;
+    return isShiftedInt<5, 1>(BrOffset);
+  case YaRISC::BCCa16:
+    // The branch address is relative to the next instruction.
+    BrOffset -= 4;
+    return isInt<16>(BrOffset);
+  }
+}
+
+MachineBasicBlock *
+YaRISCInstrInfo::getBranchDestBlock(const MachineInstr &MI) const {
+  switch (MI.getOpcode()) {
+  default:
+    llvm_unreachable("unexpected opcode!");
+  case YaRISC::BCCa5:
+  case YaRISC::BCCa16:
+    return MI.getOperand(0).getMBB();
+  }
+}
+
+bool YaRISCInstrInfo::analyzeBranch(MachineBasicBlock &MBB,
+                                    MachineBasicBlock *&TBB,
+                                    MachineBasicBlock *&FBB,
+                                    SmallVectorImpl<MachineOperand> &Cond,
+                                    bool AllowModify) const {
+  TBB = FBB = nullptr;
+  Cond.clear();
+
+  return true;
+  /* TODO: YaRISC Need to implement unconditional branch.
+  // If the block has no terminators, it just falls into the block after it.
+  MachineBasicBlock::iterator I = MBB.getLastNonDebugInstr();
+  if (I == MBB.end() || !isUnpredicatedTerminator(*I))
+    return false;
+
+  // Count the number of terminators and find the first unconditional or
+  // indirect branch.
+  MachineBasicBlock::iterator FirstUncondOrIndirectBr = MBB.end();
+  int NumTerminators = 0;
+  for (auto J = I.getReverse(); J != MBB.rend() && isUnpredicatedTerminator(*J);
+       J++) {
+    NumTerminators++;
+    if (J->getDesc().isUnconditionalBranch() ||
+        J->getDesc().isIndirectBranch()) {
+      FirstUncondOrIndirectBr = J.getReverse();
+    }
+  }
+
+  // If AllowModify is true, we can erase any terminators after
+  // FirstUncondOrIndirectBR.
+  if (AllowModify && FirstUncondOrIndirectBr != MBB.end()) {
+    while (std::next(FirstUncondOrIndirectBr) != MBB.end()) {
+      std::next(FirstUncondOrIndirectBr)->eraseFromParent();
+      NumTerminators--;
+    }
+    I = FirstUncondOrIndirectBr;
+  }
+
+  // We can't handle blocks that end in an indirect branch.
+  if (I->getDesc().isIndirectBranch())
+    return true;
+
+  // We can't handle Generic branch opcodes from Global ISel.
+  if (I->isPreISelOpcode())
+    return true;
+
+  // We can't handle blocks with more than 2 terminators.
+  if (NumTerminators > 2)
+    return true;
+
+  // Handle a single unconditional branch.
+  if (NumTerminators == 1 && I->getDesc().isUnconditionalBranch()) {
+    TBB = getBranchDestBlock(*I);
+    return false;
+  }
+
+  // Handle a single conditional branch.
+  if (NumTerminators == 1 && I->getDesc().isConditionalBranch()) {
+    parseCondBranch(*I, TBB, Cond);
+    return false;
+  }
+
+  // Handle a conditional branch followed by an unconditional branch.
+  if (NumTerminators == 2 && std::prev(I)->getDesc().isConditionalBranch() &&
+      I->getDesc().isUnconditionalBranch()) {
+    parseCondBranch(*std::prev(I), TBB, Cond);
+    FBB = getBranchDestBlock(*I);
+    return false;
+  }
+
+  // Otherwise, we can't handle this.
+  return true;
+  */
+}
+
+unsigned YaRISCInstrInfo::removeBranch(MachineBasicBlock &MBB,
+                                       int *BytesRemoved) const {
+  if (BytesRemoved)
+    *BytesRemoved = 0;
+
+  MachineBasicBlock::iterator I = MBB.end();
+  unsigned Count = 0;
+  int Removed = 0;
+  while (I != MBB.begin()) {
+    --I;
+
+    if (I->isDebugInstr())
+      continue;
+
+    if (!I->getDesc().isUnconditionalBranch() &&
+        !I->getDesc().isConditionalBranch())
+      break; // Not a branch
+
+    Removed += getInstSizeInBytes(*I);
+    I->eraseFromParent();
+    I = MBB.end();
+    ++Count;
+  }
+
+  if (BytesRemoved)
+    *BytesRemoved = Removed;
+  return Count;
+}
+
+unsigned YaRISCInstrInfo::insertBranch(
+    MachineBasicBlock &MBB, MachineBasicBlock *TBB, MachineBasicBlock *FBB,
+    ArrayRef<MachineOperand> Cond, const DebugLoc &DL, int *BytesAdded) const {
+  if (BytesAdded)
+    *BytesAdded = 0;
+
+  // Shouldn't be a fall through.
+  assert(TBB && "insertBranch must not be told to insert a fallthrough");
+  assert((Cond.size() == 2 || Cond.empty()) &&
+         "YaRISC branch conditions have two components!");
+
+  // Unconditional branch.
+  if (Cond.empty()) {
+    llvm_unreachable("not yet implemented");
+    /* TODO: YaRISC Need to implement unconditional branch.
+    MachineInstr &MI =
+        *BuildMI(&MBB, DL, get(YaRISC::BRA)).addMBB(TBB, TargetFlags);
+    if (BytesAdded)
+      *BytesAdded += getInstSizeInBytes(MI);
+    return 1;
+    */
+  }
+
+  unsigned Opc = Cond[0].getImm();
+  assert((Opc == YaRISC::BCCa16 || Opc == YaRISC::BCCa5) &&
+         "Unexpected conditional branch");
+  unsigned TargetFlags =
+      Opc == YaRISC::BCCa5 ? YaRISCII::MO_PCREL5_S1 : YaRISCII::MO_PCREL16;
+
+  // Either a one or two-way conditional branch.
+  MachineInstr &CondMI =
+      *BuildMI(&MBB, DL, get(Opc)).addMBB(TBB, TargetFlags).add(Cond[1]);
+  if (BytesAdded)
+    *BytesAdded += getInstSizeInBytes(CondMI);
+
+  // One-way conditional branch.
+  if (!FBB)
+    return 1;
+
+  // Two-way conditional branch.
+  llvm_unreachable("not yet implemented");
+  /* TODO: YaRISC Need to implement unconditional branch.
+  MachineInstr &MI =
+      *BuildMI(&MBB, DL, get(YaRISC::BRA)).addMBB(FBB, TargetFlags);
+  if (BytesAdded)
+    *BytesAdded += getInstSizeInBytes(MI);
+  return 2;
+  */
+}
+
+bool YaRISCInstrInfo::reverseBranchCondition(
+    SmallVectorImpl<MachineOperand> &Cond) const {
+  assert(Cond.size() == 2 && "Invalid branch condition!");
+
+  YaRISCCC::CondCode CC = static_cast<YaRISCCC::CondCode>(Cond[1].getImm());
+  Cond[1].setImm(getInvertedCondCode(CC));
+
+  return false;
+}
+
+std::pair<unsigned, unsigned>
+YaRISCInstrInfo::decomposeMachineOperandsTargetFlags(unsigned TF) const {
+  const unsigned Mask = YaRISCII::MO_DIRECT_FLAG_MASK;
+  return std::make_pair(TF & Mask, TF & ~Mask);
+}
+
+ArrayRef<std::pair<unsigned, const char *>>
+YaRISCInstrInfo::getSerializableDirectMachineOperandTargetFlags() const {
+  static const std::pair<unsigned, const char *> Flags[] = {
+      {YaRISCII::MO_CALL, "yarisc-call"},
+      {YaRISCII::MO_CALL_PLT, "yarisc-call-plt"},
+  };
+  return Flags;
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCInstrInfo.h b/llvm/lib/Target/YaRISC/YaRISCInstrInfo.h
new file mode 100644
index 000000000000..208c13cf5582
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCInstrInfo.h
@@ -0,0 +1,103 @@
+//===-- YaRISCInstrInfo.h - YaRISC Instruction Information ------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains the YaRISC implementation of the TargetInstrInfo class.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_YARISCINSTRINFO_H
+#define LLVM_LIB_TARGET_YARISC_YARISCINSTRINFO_H
+
+#include "YaRISCRegisterInfo.h"
+#include "llvm/CodeGen/TargetInstrInfo.h"
+
+#define GET_INSTRINFO_HEADER
+#include "YaRISCGenInstrInfo.inc"
+
+namespace llvm {
+
+namespace YaRISCII {
+enum {
+  MO_None,
+  MO_CALL,
+  MO_CALL_PLT,
+  MO_PCREL16,
+  MO_PCREL5_S1,
+  MO_PCREL9_S1,
+
+  // Used to differentiate between target-specific "direct" flags and "bitmask"
+  // flags. A machine operand can only have one "direct" flag, but can have
+  // multiple "bitmask" flags.
+  MO_DIRECT_FLAG_MASK = 0x07,
+};
+} // namespace YaRISCII
+
+class YaRISCSubtarget;
+
+class YaRISCInstrInfo : public YaRISCGenInstrInfo {
+  const YaRISCRegisterInfo RI;
+  const YaRISCSubtarget &Subtarget;
+  virtual void anchor();
+
+public:
+  explicit YaRISCInstrInfo(const YaRISCSubtarget &ST);
+
+  const YaRISCRegisterInfo &getRegisterInfo() const { return RI; }
+
+  unsigned getInstSizeInBytes(const MachineInstr &MI) const override;
+
+  void copyPhysReg(MachineBasicBlock &MBB, MachineBasicBlock::iterator MBBI,
+                   const DebugLoc &DL, Register DestReg, Register SrcReg,
+                   bool KillSrc, bool RenamableDest = false,
+                   bool RenamableSrc = false) const override;
+
+  void storeRegToStackSlot(
+      MachineBasicBlock &MBB, MachineBasicBlock::iterator MBBI, Register SrcReg,
+      bool KillSrc, int FrameIndex, const TargetRegisterClass *RC,
+      Register VReg,
+      MachineInstr::MIFlag Flags = MachineInstr::NoFlags) const override;
+
+  void loadRegFromStackSlot(
+      MachineBasicBlock &MBB, MachineBasicBlock::iterator MBBI,
+      Register DestReg, int FrameIndex, const TargetRegisterClass *RC,
+      Register VReg,
+      MachineInstr::MIFlag Flags = MachineInstr::NoFlags) const override;
+
+  bool expandPostRAPseudo(MachineInstr &MI) const override;
+
+  bool isBranchOffsetInRange(unsigned BranchOp,
+                             int64_t BrOffset) const override;
+
+  MachineBasicBlock *getBranchDestBlock(const MachineInstr &MI) const override;
+
+  bool analyzeBranch(MachineBasicBlock &MBB, MachineBasicBlock *&TBB,
+                     MachineBasicBlock *&FBB,
+                     SmallVectorImpl<MachineOperand> &Cond,
+                     bool AllowModify = false) const override;
+
+  unsigned removeBranch(MachineBasicBlock &MBB,
+                        int *BytesRemoved = nullptr) const override;
+
+  unsigned insertBranch(MachineBasicBlock &MBB, MachineBasicBlock *TBB,
+                        MachineBasicBlock *FBB, ArrayRef<MachineOperand> Cond,
+                        const DebugLoc &DL,
+                        int *BytesAdded = nullptr) const override;
+
+  bool
+  reverseBranchCondition(SmallVectorImpl<MachineOperand> &Cond) const override;
+
+  std::pair<unsigned, unsigned>
+  decomposeMachineOperandsTargetFlags(unsigned TF) const override;
+
+  ArrayRef<std::pair<unsigned, const char *>>
+  getSerializableDirectMachineOperandTargetFlags() const override;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_YARISCINSTRINFO_H
diff --git a/llvm/lib/Target/YaRISC/YaRISCInstrInfo.td b/llvm/lib/Target/YaRISC/YaRISCInstrInfo.td
new file mode 100644
index 000000000000..7f4b2cbe64f9
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCInstrInfo.td
@@ -0,0 +1,406 @@
+//===-- YaRISCInstrInfo.td - Target Description for YaRISC Target ---------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file describes the YaRISC instructions in TableGen format.
+//
+//===----------------------------------------------------------------------===//
+
+//===----------------------------------------------------------------------===//
+// YaRISC specific DAG Nodes
+//===----------------------------------------------------------------------===//
+
+def SDT_CallSeqStart : SDCallSeqStart<[
+  SDTCisVT<0, i16>, // amount
+  SDTCisVT<1, i16>, // alignment
+]>;
+
+def SDT_CallSeqEnd : SDCallSeqEnd<[
+  SDTCisVT<0, i16>, // amount
+  SDTCisVT<1, i16>, // alignment
+]>;
+
+def SDT_YaRISCCall : SDTypeProfile<0, -1, [
+  SDTCisVT<0, iPTR>, // call target
+]>;
+
+def SDT_YaRISCCmp : SDTypeProfile<0, 2, [
+  SDTCisSameAs<0, 1>, // lhs and rhs
+]>;
+
+def SDT_YaRISCBrcond : SDTypeProfile<0, 2, [
+  SDTCisVT<0, OtherVT>, // branch target basic block
+  SDTCisVT<1, i16>,     // condition code
+]>;
+
+def SDT_YaRISCSelectCC : SDTypeProfile<1, 3, [
+  SDTCisSameAs<0, 1>, // selected output value
+  SDTCisSameAs<1, 2>, // true and false input value
+  SDTCisVT<3, i16>,   // condition code
+]>;
+
+def CallSeqStart : SDNode<"ISD::CALLSEQ_START", SDT_CallSeqStart,
+                          [SDNPHasChain, SDNPOutGlue]>;
+def CallSeqEnd : SDNode<"ISD::CALLSEQ_END", SDT_CallSeqEnd,
+                        [SDNPHasChain, SDNPOptInGlue, SDNPOutGlue]>;
+
+def YaRISCCall : SDNode<"YaRISCISD::CALL", SDT_YaRISCCall,
+                        [SDNPHasChain, SDNPOutGlue, SDNPOptInGlue,
+                         SDNPVariadic]>;
+def YaRISCRetGlue : SDNode<"YaRISCISD::RET_GLUE", SDTNone,
+                           [SDNPHasChain, SDNPOptInGlue, SDNPVariadic]>;
+
+def YaRISCCmp : SDNode<"YaRISCISD::CMP", SDT_YaRISCCmp, [SDNPOutGlue]>;
+def YaRISCBrcond : SDNode<"YaRISCISD::BRCOND", SDT_YaRISCBrcond,
+                          [SDNPHasChain, SDNPInGlue]>;
+def YaRISCSelectCC : SDNode<"YaRISCISD::SELECT_CC", SDT_YaRISCSelectCC,
+                            [SDNPInGlue]>;
+
+//===----------------------------------------------------------------------===//
+// Instruction Formats
+//===----------------------------------------------------------------------===//
+
+include "YaRISCInstrFormats.td"
+
+//===----------------------------------------------------------------------===//
+// Feature Predicates
+//===----------------------------------------------------------------------===//
+
+// HasV1 - This predicate is true when the target processor supports V1
+// instructions. TODO: YaRISC
+def HasV1 : Predicate<"Subtarget->isV1()">,
+            AssemblerPredicate<(all_of FeatureV1)>;
+
+//===----------------------------------------------------------------------===//
+// Instruction Pattern Stuff
+//===----------------------------------------------------------------------===//
+
+def ccode : Operand<i16> {
+  let PrintMethod = "printCondCode";
+  let OperandNamespace = "YaRISCOp";
+  let OperandType = "OPERAND_COND_CODE";
+}
+
+class SImmOperand<int N>
+  : Operand<i16>, ImmLeaf<i16, "return isInt<"#N#">(Imm);"> {
+  let EncoderMethod = "getImmOpValue";
+  let DecoderMethod = "decodeSImmOperand<"#N#">";
+}
+
+class UImmOperand<int N>
+  : Operand<i16>, ImmLeaf<i16, "return isUInt<"#N#">(Imm);"> {
+  let EncoderMethod = "getImmOpValue";
+  let DecoderMethod = "decodeUImmOperand<"#N#">";
+}
+
+def simm4 : SImmOperand<4>;
+def uimm16 : UImmOperand<16>;
+
+class BranchOperand<int N, int S = 0>
+  : Operand<OtherVT>, ImmLeaf<i16, "return isShiftedInt<"#N#", "#S#">(Imm);"> {
+  let EncoderMethod = "getBranchTargetOpValue<"#N#", "#S#">";
+  let OperandType = "OPERAND_PCREL";
+  let MCOperandPredicate = [{
+    int64_t Imm;
+    if (MCOp.evaluateAsConstantImm(Imm))
+      return isShiftedInt<}]#N#[{,}]#S#[{>(Imm);
+    return MCOp.isBareSymbolRef();
+  }];
+}
+
+def brtarget16 : BranchOperand<16>;
+def brtarget5 : BranchOperand<5, 1>;
+def brtarget9 : BranchOperand<9, 1>;
+
+def calltarget : Operand<iPTR>;
+
+def yarisc_ptr_rc : RegisterClass<"YaRISC", [i16], 16, (add GPR)>;
+
+def ptr_op : RegisterOperand<yarisc_ptr_rc> {
+  let DecoderMethod = "DecodeGPRRegisterClass";
+}
+
+//===----------------------------------------------------------------------===//
+// Flag Conditions
+//===----------------------------------------------------------------------===//
+
+class CC_VAL<bits<4> Enc, string Name> {
+  bits<4> Encoding = Enc;
+  string Mnemonic = Name;
+}
+
+def CC_VALTable : GenericTable {
+  let FilterClass = "CC_VAL";
+  let CppTypeName = "CondCodeInfo";
+  let Fields = ["Encoding", "Mnemonic"];
+  let PrimaryKey = ["Encoding"];
+  let PrimaryKeyName = "lookupMnemonicByEncoding";
+}
+
+def CC_EQ : CC_VAL<0b0001, "EQ">;
+def CC_LT : CC_VAL<0b0010, "LT">;
+def CC_LE : CC_VAL<0b0011, "LE">;
+def CC_LO : CC_VAL<0b0100, "LO">;
+def CC_LS : CC_VAL<0b0101, "LS">;
+def CC_NE : CC_VAL<0b1001, "NE">;
+def CC_GE : CC_VAL<0b1010, "GE">;
+def CC_GT : CC_VAL<0b1011, "GT">;
+def CC_HS : CC_VAL<0b1100, "HS">;
+def CC_HI : CC_VAL<0b1101, "HI">;
+
+//===----------------------------------------------------------------------===//
+// Instruction Class Templates
+//===----------------------------------------------------------------------===//
+
+// TODO: YaRISC Consider generalization with memory operands.
+
+multiclass Move<bits<6> op, string opstr> {
+  def r : InstOp2Reg<op, (outs GPR:$op0), (ins GPR:$op1),
+                     !strconcat(opstr, " $op0, $op1"), []>;
+
+  let AddedComplexity = 1 in
+  def i4 : InstOp2RegImm4<op, (outs GPR:$op0), (ins simm4:$imm),
+                          !strconcat(opstr, " $op0, $imm"),
+                          [(set GPR:$op0, simm4:$imm)]>;
+
+  def i16 : InstOp2RegImm16<op, (outs GPR:$op0), (ins uimm16:$imm),
+                            !strconcat(opstr, " $op0, $imm"),
+                            [(set GPR:$op0, uimm16:$imm)]>;
+}
+
+multiclass Load<bits<6> op, string opstr> {
+  def r : InstOp2Reg<op, (outs GPR:$op0), (ins GPR:$op1),
+                     !strconcat(opstr, " $op0, [$op1]"),
+                     [(set GPR:$op0, (load GPR:$op1))]>;
+
+  let AddedComplexity = 1 in
+  def i4 : InstOp2RegImm4<op, (outs GPR:$op0), (ins simm4:$imm),
+                          !strconcat(opstr, " $op0, [$imm]"),
+                          [(set GPR:$op0, (load simm4:$imm))]>;
+
+  def i16 : InstOp2RegImm16<op, (outs GPR:$op0), (ins uimm16:$imm),
+                            !strconcat(opstr, " $op0, [$imm]"),
+                            [(set GPR:$op0, (load uimm16:$imm))]>;
+}
+
+multiclass IndexedLoad<bits<6> op, string opstr> {
+  def rr : InstOp3Reg<op, (outs GPR:$op0), (ins GPR:$op1, GPR:$op2),
+                      !strconcat(opstr, " $op0, [$op1, $op2]"),
+                      [(set GPR:$op0, (load (add GPR:$op1, GPR:$op2)))]>;
+
+  def ri16 : InstOp3RegImm16<op, (outs GPR:$op0), (ins GPR:$op1, uimm16:$imm),
+                             !strconcat(opstr, " $op0, [$op1, $imm]"),
+                             [(set GPR:$op0,
+                               (load (add GPR:$op1, uimm16:$imm)))]>;
+
+  let Uses = [SP], AddedComplexity = 1 in
+  def spi4 : InstOp3RegImm4<op, (outs GPR:$op0), (ins simm4:$imm),
+                            !strconcat(opstr, " $op0, [sp, $imm]"),
+                            [(set GPR:$op0, (load (add SP, simm4:$imm)))]>;
+
+  let Uses = [R5], AddedComplexity = 1 in
+  def fpi4 : InstOp3Imm4Reg<op, (outs GPR:$op0), (ins simm4:$imm),
+                            !strconcat(opstr, " $op0, [r5, $imm]"),
+                            [(set GPR:$op0, (load (add R5, simm4:$imm)))]>;
+}
+
+multiclass Store<bits<6> op, string opstr> {
+  def r : InstOp2Reg<op, (outs), (ins GPR:$op0, GPR:$op1),
+                     !strconcat(opstr, " $op0, [$op1]"),
+                     [(store GPR:$op0, GPR:$op1)]>;
+
+  let AddedComplexity = 1 in
+  def i4 : InstOp2RegImm4<op, (outs), (ins GPR:$op0, simm4:$imm),
+                          !strconcat(opstr, " $op0, [$imm]"),
+                          [(store GPR:$op0, simm4:$imm)]>;
+
+  def i16 : InstOp2RegImm16<op, (outs), (ins GPR:$op0, uimm16:$imm),
+                            !strconcat(opstr, " $op0, [$imm]"),
+                            [(store GPR:$op0, uimm16:$imm)]>;
+}
+
+multiclass IndexedStore<bits<6> op, string opstr> {
+  def rr : InstOp3Reg<op, (outs GPR:$op0), (ins GPR:$op1, GPR:$op2),
+                      !strconcat(opstr, " $op0, [$op1, $op2]"),
+                      [(store GPR:$op0, (add GPR:$op1, GPR:$op2))]>;
+
+  def ri16 : InstOp3RegImm16<op, (outs), (ins GPR:$op0, GPR:$op1, uimm16:$imm),
+                             !strconcat(opstr, " $op0, [$op1, $imm]"),
+                             [(store GPR:$op0, (add GPR:$op1, uimm16:$imm))]>;
+
+  let Uses = [SP], AddedComplexity = 1 in
+  def spi4 : InstOp3RegImm4<op, (outs), (ins GPR:$op0, simm4:$imm),
+                            !strconcat(opstr, " $op0, [sp, $imm]"),
+                            [(store GPR:$op0, (add SP, simm4:$imm))]>;
+
+  let Uses = [R5], AddedComplexity = 1 in
+  def fpi4 : InstOp3Imm4Reg<op, (outs), (ins GPR:$op0, simm4:$imm),
+                            !strconcat(opstr, " $op0, [r5, $imm]"),
+                            [(store GPR:$op0, (add R5, simm4:$imm))]>;
+}
+
+multiclass UnaryArith<bits<6> op, string opstr, SDNode opnode> {
+  def r : InstOp2Reg<op, (outs GPR:$op0), (ins GPR:$op1),
+                     !strconcat(opstr, " $op0, $op1"),
+                     [(set GPR:$op0, (opnode GPR:$op1))]>;
+
+  let AddedComplexity = 1 in
+  def i4 : InstOp2RegImm4<op, (outs GPR:$op0), (ins simm4:$imm),
+                          !strconcat(opstr, " $op0, $imm"),
+                          [(set GPR:$op0, (opnode simm4:$imm))]>;
+
+  def i16 : InstOp2RegImm16<op, (outs GPR:$op0), (ins uimm16:$imm),
+                            !strconcat(opstr, " $op0, $imm"),
+                            [(set GPR:$op0, (opnode uimm16:$imm))]>;
+}
+
+multiclass BinaryArith<bits<6> op, string opstr, SDNode opnode> {
+  def rr : InstOp3Reg<op, (outs GPR:$op0), (ins GPR:$op1, GPR:$op2),
+                      !strconcat(opstr, " $op0, $op1, $op2"),
+                      [(set GPR:$op0, (opnode GPR:$op1, GPR:$op2))]>;
+
+  let Constraints = "$op0 = $op1", AddedComplexity = 1 in
+  def ri4 : InstOp3RegImm4<op, (outs GPR:$op0), (ins GPR:$op1, simm4:$imm),
+                           !strconcat(opstr, " $op0, $op1, $imm"),
+                           [(set GPR:$op0, (opnode GPR:$op1, simm4:$imm))]>;
+
+  def ri16 : InstOp3RegImm16<op, (outs GPR:$op0), (ins GPR:$op1, uimm16:$imm),
+                             !strconcat(opstr, " $op0, $op1, $imm"),
+                             [(set GPR:$op0, (opnode GPR:$op1, uimm16:$imm))]>;
+}
+
+multiclass AsymmetricArith<bits<6> op, string opstr, SDNode opnode>
+  : BinaryArith<op, opstr, opnode> {
+  let Constraints = "$op0 = $op2", AddedComplexity = 1 in
+  def i4r : InstOp3Imm4Reg<op, (outs GPR:$op0), (ins simm4:$imm, GPR:$op2),
+                           !strconcat(opstr, " $op0, $imm, $op2"), []>;
+
+  def i16r : InstOp3Imm16Reg<op, (outs GPR:$op0), (ins uimm16:$imm, GPR:$op2),
+                             !strconcat(opstr, " $op0, $imm, $op2"), []>;
+}
+
+multiclass Comparison<bits<6> op, string opstr, SDNode opnode> {
+  def rr : InstOp2Reg<op, (outs), (ins GPR:$op0, GPR:$op1),
+                      !strconcat(opstr, " $op0, $op1"),
+                      [(opnode GPR:$op0, GPR:$op1)]>;
+
+  let AddedComplexity = 1 in
+  def ri4 : InstOp2RegImm4<op, (outs), (ins GPR:$op0, simm4:$imm),
+                           !strconcat(opstr, " $op0, $imm"),
+                           [(opnode GPR:$op0, simm4:$imm)]>;
+
+  def ri16 : InstOp2RegImm16<op, (outs), (ins GPR:$op0, uimm16:$imm),
+                             !strconcat(opstr, " $op0, $imm"),
+                             [(opnode GPR:$op0, uimm16:$imm)]>;
+
+  let AddedComplexity = 1 in
+  def i4r : InstOp2Imm4Reg<op, (outs), (ins simm4:$imm, GPR:$op0),
+                           !strconcat(opstr, " $imm, $op0"),
+                           [(opnode simm4:$imm, GPR:$op0)]>;
+
+  def i16r : InstOp2Imm16Reg<op, (outs), (ins uimm16:$imm, GPR:$op0),
+                             !strconcat(opstr, " $imm, $op0"),
+                             [(opnode uimm16:$imm, GPR:$op0)]>;
+}
+
+multiclass CondBranch<bits<6> op, string opstr, SDNode opnode> {
+  let AddedComplexity = 1 in
+  def a5 : InstBranchCond5<op, (outs), (ins brtarget5:$addr, ccode:$cc),
+                           !strconcat(!subst("#", "$cc", opstr), " $addr"),
+                           [(opnode bb:$addr, imm:$cc)]>;
+
+  def a16 : InstBranchCond16<op, (outs), (ins brtarget16:$addr, ccode:$cc),
+                             !strconcat(!subst("#", "$cc", opstr), " $addr"),
+                             [(opnode bb:$addr, imm:$cc)]>;
+}
+
+//===----------------------------------------------------------------------===//
+// Instructions
+//===----------------------------------------------------------------------===//
+
+defm MOV : Move<0b000001, "MOV">;
+
+let mayLoad = 1 in {
+  defm LDR : Load<0b000010, "LDR">;
+  defm LDX : IndexedLoad<0b000011, "LDX">;
+}
+
+let mayStore = 1 in {
+  defm STR : Store<0b000100, "STR">;
+  defm STX : IndexedStore<0b000101, "STX">;
+}
+
+defm ADD : BinaryArith<0b010000, "ADD", add>;
+defm SUB : AsymmetricArith<0b010100, "SUB", sub>;
+// TODO: YaRISC Not yet assigned.
+defm XOR : BinaryArith<0b111100, "XOR", xor>;
+
+let Defs = [NZCV] in {
+  defm ADDS : BinaryArith<0b010001, "ADDS", add>;
+  defm SUBS : AsymmetricArith<0b010101, "SUBS", sub>;
+  defm CMP : Comparison<0b101010, "CMP", YaRISCCmp>;
+}
+
+let Uses = [NZCV] in {
+  defm BCC : CondBranch<0b101100, "B#", YaRISCBrcond>;
+}
+
+def NOP : InstOp0<0b111111, (outs), (ins), "NOP", []>;
+
+def SUBreg : PatFrag<(ops node:$rs, node:$imm),
+                     (add (sub 0, node:$rs), node:$imm)>;
+
+def : Pat<(SUBreg GPR:$rs, uimm16:$imm),
+          (SUBi16r imm:$imm, GPR:$rs)>;
+def : Pat<(SUBreg GPR:$rs, simm4:$imm),
+          (SUBi4r imm:$imm, GPR:$rs)>;
+
+def to_tframeindex : SDNodeXForm<frameindex, [{
+  return CurDAG->getTargetFrameIndex(N->getIndex(), N->getValueType(0));
+}]>;
+
+def : Pat<(frameindex:$fi),
+          (ADDri16 (iPTR (to_tframeindex $fi)), 0)>;
+def : Pat<(add frameindex:$fi, imm:$offset),
+          (ADDri16 (iPTR (to_tframeindex $fi)), i16imm:$offset)>;
+
+//===----------------------------------------------------------------------===//
+// Pseudo-instructions
+//===----------------------------------------------------------------------===//
+
+class Pseudo<dag outs, dag ins, string asmstr, list<dag> pattern>
+   : InstYaRISC<0, outs, ins, asmstr, pattern> {
+  let isCodeGenOnly = 1;
+  let isPseudo = 1;
+}
+
+let Defs = [SP], Uses = [SP] in {
+  def ADJCALLSTACKDOWN : Pseudo<(outs), (ins i16imm:$amt, i16imm:$align),
+                                "#ADJCALLSTACKDOWN $amt $align",
+                                [(CallSeqStart timm:$amt, timm:$align)]>;
+  def ADJCALLSTACKUP : Pseudo<(outs), (ins i16imm:$amt, i16imm:$align),
+                              "#ADJCALLSTACKUP $amt $align",
+                              [(CallSeqEnd timm:$amt, timm:$align)]>;
+}
+
+let Uses = [SP] in {
+  // Register R2 is used to calculate the return address.
+  let Defs = [SP, IP, R2], isCall = 1 in
+  def CALL : Pseudo<(outs), (ins calltarget:$target), "#CALL $target", []>;
+  let Defs = [SP, IP], isTerminator = 1, isReturn = 1, isBarrier = 1 in
+  def RET : Pseudo<(outs), (ins), "#RET", []>;
+}
+
+def : Pat<(YaRISCCall tglobaladdr:$target), (CALL tglobaladdr:$target)>;
+def : Pat<(YaRISCCall texternalsym:$target), (CALL texternalsym:$target)>;
+def : Pat<(YaRISCRetGlue), (RET)>;
+
+let Uses = [NZCV], usesCustomInserter = 1 in
+def SELECT : Pseudo<(outs GPR:$dst), (ins GPR:$lhs, GPR:$rhs, ccode:$cc),
+                    "#SELECT $lhs, $rhs, $cc",
+                    [(set i16:$dst,
+                      (YaRISCSelectCC i16:$lhs, i16:$rhs, imm:$cc))]>;
diff --git a/llvm/lib/Target/YaRISC/YaRISCRegisterInfo.cpp b/llvm/lib/Target/YaRISC/YaRISCRegisterInfo.cpp
new file mode 100644
index 000000000000..7d9565fd52a4
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCRegisterInfo.cpp
@@ -0,0 +1,142 @@
+//===-- YaRISCRegisterInfo.cpp - YaRISC Register Information --------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains the YaRISC implementation of the TargetRegisterInfo class.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCRegisterInfo.h"
+#include "YaRISC.h"
+#include "YaRISCSubtarget.h"
+#include "llvm/ADT/BitVector.h"
+#include "llvm/CodeGen/MachineFrameInfo.h"
+#include "llvm/CodeGen/MachineFunction.h"
+#include "llvm/CodeGen/MachineInstrBuilder.h"
+#include "llvm/CodeGen/TargetInstrInfo.h"
+#include "llvm/IR/Type.h"
+#include "llvm/Support/CommandLine.h"
+#include "llvm/Support/ErrorHandling.h"
+#include "llvm/Support/MathExtras.h"
+
+using namespace llvm;
+
+#define GET_REGINFO_TARGET_DESC
+#include "YaRISCGenRegisterInfo.inc"
+
+YaRISCRegisterInfo::YaRISCRegisterInfo(const YaRISCSubtarget &STI)
+    // TODO: YaRISC SP as CFA?
+    : YaRISCGenRegisterInfo(YaRISC::NoRegister, 0, 0, YaRISC::IP) {}
+
+const MCPhysReg *
+YaRISCRegisterInfo::getCalleeSavedRegs(const MachineFunction *MF) const {
+  return CSR_SaveList;
+}
+
+const uint32_t *
+YaRISCRegisterInfo::getCallPreservedMask(const MachineFunction &MF,
+                                         CallingConv::ID CC) const {
+  return CSR_RegMask;
+}
+
+BitVector YaRISCRegisterInfo::getReservedRegs(const MachineFunction &MF) const {
+  const TargetFrameLowering *TFI = MF.getSubtarget().getFrameLowering();
+
+  BitVector Reserved(getNumRegs());
+  Reserved.set(YaRISC::SP); // stack pointer
+  Reserved.set(YaRISC::IP); // program counter
+
+  if (TFI->hasFP(MF))
+    Reserved.set(YaRISC::R5); // frame pointer
+
+  assert(checkAllSuperRegsMarked(Reserved));
+  return Reserved;
+}
+
+const TargetRegisterClass *
+YaRISCRegisterInfo::getPointerRegClass(unsigned Kind) const {
+  assert(Kind == 0 && "this should only be used for default cases");
+  return &YaRISC::GPRRegClass;
+}
+
+bool YaRISCRegisterInfo::eliminateFrameIndex(MachineBasicBlock::iterator II,
+                                             int SPAdj, unsigned FIOperandNum,
+                                             RegScavenger *RS) const {
+  MachineInstr &MI = *II;
+  MachineBasicBlock &MBB = *MI.getParent();
+  MachineFunction &MF = *MBB.getParent();
+  const TargetFrameLowering *TFI = MF.getSubtarget().getFrameLowering();
+  assert(MI.getOpcode() == YaRISC::LDXri16 ||
+         MI.getOpcode() == YaRISC::STXri16 ||
+         MI.getOpcode() == YaRISC::ADDri16);
+
+  int FrameIndex = MI.getOperand(FIOperandNum).getIndex();
+
+  Register FrameReg;
+  StackOffset Offset = TFI->getFrameIndexReference(MF, FrameIndex, FrameReg);
+  int64_t RealOffset =
+      Offset.getFixed() + MI.getOperand(FIOperandNum + 1).getImm();
+
+  bool IsFramePointer = FrameReg == YaRISC::R5;
+  bool IsStackPointer = FrameReg == YaRISC::SP;
+  if (IsStackPointer)
+    RealOffset += SPAdj;
+
+  if (isInt<4>(RealOffset)) {
+    const TargetInstrInfo &TII = *MF.getSubtarget().getInstrInfo();
+    Register Reg = MI.getOperand(0).getReg();
+    DebugLoc DL = MI.getDebugLoc();
+
+    // There are special stack pointer and frame pointer relative loads and
+    // stores for small offsets that fit into one word.
+    if ((IsFramePointer || IsStackPointer) &&
+        (MI.getOpcode() == YaRISC::LDXri16 ||
+         MI.getOpcode() == YaRISC::STXri16)) {
+      unsigned Opcode =
+          MI.getOpcode() == YaRISC::LDXri16
+              ? (IsStackPointer ? YaRISC::LDXspi4 : YaRISC::LDXfpi4)
+              : (IsStackPointer ? YaRISC::STXspi4 : YaRISC::STXfpi4);
+
+      BuildMI(MBB, MI, DL, TII.get(Opcode), Reg).addImm(RealOffset);
+
+      MBB.erase(II);
+      return true;
+    }
+
+    if (MI.getOpcode() == YaRISC::ADDri16) {
+      // Pick a smaller instruction when the source and target are the same or
+      // when the offset is zero. It is unknown whether the registers are ever
+      // the same seems very odd (the frame pointer is probably not being reused
+      // as general purpose register), however for now we just leave it in.
+      if (Reg == FrameReg) {
+        if (RealOffset != 0) {
+          BuildMI(MBB, MI, DL, TII.get(YaRISC::ADDri4), Reg)
+              .addReg(FrameReg)
+              .addImm(RealOffset);
+        }
+
+        MBB.erase(II);
+        return true;
+      } else if (RealOffset == 0) {
+        BuildMI(MBB, MI, DL, TII.get(YaRISC::MOVr), Reg).addReg(FrameReg);
+
+        MBB.erase(II);
+        return true;
+      }
+    }
+  }
+
+  MI.getOperand(FIOperandNum).ChangeToRegister(FrameReg, false);
+  MI.getOperand(FIOperandNum + 1).ChangeToImmediate(RealOffset);
+  // II is not removed.
+  return false;
+}
+
+Register YaRISCRegisterInfo::getFrameRegister(const MachineFunction &MF) const {
+  const TargetFrameLowering *TFI = MF.getSubtarget().getFrameLowering();
+  return TFI->hasFP(MF) ? YaRISC::R5 : YaRISC::SP;
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCRegisterInfo.h b/llvm/lib/Target/YaRISC/YaRISCRegisterInfo.h
new file mode 100644
index 000000000000..6a049d822298
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCRegisterInfo.h
@@ -0,0 +1,45 @@
+//===-- YaRISCRegisterInfo.h - YaRISC Register Information Impl -*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file contains the YaRISC implementation of the TargetRegisterInfo class.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_YARISCREGISTERINFO_H
+#define LLVM_LIB_TARGET_YARISC_YARISCREGISTERINFO_H
+
+#include "llvm/CodeGen/TargetRegisterInfo.h"
+
+#define GET_REGINFO_HEADER
+#include "YaRISCGenRegisterInfo.inc"
+
+namespace llvm {
+class YaRISCSubtarget;
+
+struct YaRISCRegisterInfo : public YaRISCGenRegisterInfo {
+public:
+  explicit YaRISCRegisterInfo(const YaRISCSubtarget &STI);
+
+  const MCPhysReg *getCalleeSavedRegs(const MachineFunction *MF) const override;
+  const uint32_t *getCallPreservedMask(const MachineFunction &MF,
+                                       CallingConv::ID CC) const override;
+
+  BitVector getReservedRegs(const MachineFunction &MF) const override;
+
+  const TargetRegisterClass *getPointerRegClass(unsigned Kind) const override;
+
+  bool eliminateFrameIndex(MachineBasicBlock::iterator II, int SPAdj,
+                           unsigned FIOperandNum,
+                           RegScavenger *RS = nullptr) const override;
+
+  Register getFrameRegister(const MachineFunction &MF) const override;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_YARISCREGISTERINFO_H
diff --git a/llvm/lib/Target/YaRISC/YaRISCRegisterInfo.td b/llvm/lib/Target/YaRISC/YaRISCRegisterInfo.td
new file mode 100644
index 000000000000..8b71851e5b56
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCRegisterInfo.td
@@ -0,0 +1,44 @@
+//===-- YaRISCRegisterInfo.td - YaRISC Register defs -------*- tablegen -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+//===----------------------------------------------------------------------===//
+//  Declarations that describe the YaRISC register file
+//===----------------------------------------------------------------------===//
+
+// Registers are identified with 3-bit ID numbers.
+class YaRISCReg<bits<3> enc, string n> : Register<n> {
+  let HWEncoding{2-0} = enc;
+  let Namespace = "YaRISC";
+}
+
+class YaRISCCtrlReg<string n>: Register<n> {
+  let Namespace = "YaRISC";
+}
+
+// General purpose registers
+def R0 : YaRISCReg<0, "r0">, DwarfRegNum<[0]>;
+def R1 : YaRISCReg<1, "r1">, DwarfRegNum<[1]>;
+def R2 : YaRISCReg<2, "r2">, DwarfRegNum<[2]>;
+def R3 : YaRISCReg<3, "r3">, DwarfRegNum<[3]>;
+def R4 : YaRISCReg<4, "r4">, DwarfRegNum<[4]>;
+def R5 : YaRISCReg<5, "r5">, DwarfRegNum<[5]>;
+def SP : YaRISCReg<6, "sp">, DwarfRegNum<[6]>;
+def IP : YaRISCReg<7, "ip">, DwarfRegNum<[7]>;
+
+// Register class for the general purpose registers.
+def GPR : RegisterClass<"YaRISC", [i16], 16,
+                        (add R0, R1, R2, R3, R4, R5, SP, IP)>;
+
+// Status register (negative/zero/carry/overflow)
+def NZCV : YaRISCCtrlReg<"NZCV">;
+
+// Register class for the status register.
+def CCR : RegisterClass<"YaRISC", [i16], 16, (add NZCV)> {
+  let CopyCost = -1; // Don't allow copying of status registers.
+  let isAllocatable = 0;
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCSelectionDAGInfo.cpp b/llvm/lib/Target/YaRISC/YaRISCSelectionDAGInfo.cpp
new file mode 100644
index 000000000000..876bd51df82e
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCSelectionDAGInfo.cpp
@@ -0,0 +1,19 @@
+//===----------------------------------------------------------------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCSelectionDAGInfo.h"
+
+#define GET_SDNODE_DESC
+#include "YaRISCGenSDNodeInfo.inc"
+
+using namespace llvm;
+
+YaRISCSelectionDAGInfo::YaRISCSelectionDAGInfo()
+    : SelectionDAGGenTargetInfo(YaRISCGenSDNodeInfo) {}
+
+YaRISCSelectionDAGInfo::~YaRISCSelectionDAGInfo() = default;
diff --git a/llvm/lib/Target/YaRISC/YaRISCSelectionDAGInfo.h b/llvm/lib/Target/YaRISC/YaRISCSelectionDAGInfo.h
new file mode 100644
index 000000000000..16affe0579a4
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCSelectionDAGInfo.h
@@ -0,0 +1,28 @@
+//===----------------------------------------------------------------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_YARISCSELECTIONDAGINFO_H
+#define LLVM_LIB_TARGET_YARISC_YARISCSELECTIONDAGINFO_H
+
+#include "llvm/CodeGen/SelectionDAGTargetInfo.h"
+
+#define GET_SDNODE_ENUM
+#include "YaRISCGenSDNodeInfo.inc"
+
+namespace llvm {
+
+class YaRISCSelectionDAGInfo : public SelectionDAGGenTargetInfo {
+public:
+  YaRISCSelectionDAGInfo();
+
+  ~YaRISCSelectionDAGInfo() override;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_YARISCSELECTIONDAGINFO_H
diff --git a/llvm/lib/Target/YaRISC/YaRISCSubtarget.cpp b/llvm/lib/Target/YaRISC/YaRISCSubtarget.cpp
new file mode 100644
index 000000000000..7274545f7aea
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCSubtarget.cpp
@@ -0,0 +1,96 @@
+//===-- YaRISCSubtarget.cpp - YaRISC Subtarget Information ----------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file implements the YaRISC specific subclass of TargetSubtargetInfo.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCSubtarget.h"
+#include "YaRISCSelectionDAGInfo.h"
+#include "llvm/ADT/StringRef.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/MathExtras.h"
+#include "llvm/Target/TargetMachine.h"
+
+using namespace llvm;
+
+#define DEBUG_TYPE "yarisc-subtarget"
+
+#define GET_SUBTARGETINFO_TARGET_DESC
+#define GET_SUBTARGETINFO_CTOR
+#include "YaRISCGenSubtargetInfo.inc"
+
+void YaRISCSubtarget::anchor() {}
+
+YaRISCSubtarget &YaRISCSubtarget::initializeSubtargetDependencies(
+    StringRef CPU, StringRef TuneCPU, StringRef FS) {
+  // Determine default and user specified characteristics
+  std::string CPUName = std::string(CPU);
+  if (CPUName.empty())
+    CPUName = "minimal";
+
+  if (TuneCPU.empty())
+    TuneCPU = CPUName;
+
+  // Parse features string.
+  ParseSubtargetFeatures(CPUName, TuneCPU, FS);
+
+  return *this;
+}
+
+YaRISCSubtarget::YaRISCSubtarget(const StringRef &CPU, const StringRef &TuneCPU,
+                                 const StringRef &FS, const TargetMachine &TM)
+    : YaRISCGenSubtargetInfo(TM.getTargetTriple(), CPU, TuneCPU, FS),
+      ReserveRegister(TM.getMCRegisterInfo()->getNumRegs()),
+      InstrInfo(initializeSubtargetDependencies(CPU, TuneCPU, FS)),
+      TLInfo(TM, *this), FrameLowering(*this) {
+  TSInfo = std::make_unique<YaRISCSelectionDAGInfo>();
+}
+
+YaRISCSubtarget::~YaRISCSubtarget() = default;
+
+const SelectionDAGTargetInfo *YaRISCSubtarget::getSelectionDAGInfo() const {
+  return TSInfo.get();
+}
+
+void YaRISCSubtarget::initLibcallLoweringInfo(LibcallLoweringInfo &Info) const {
+  // Setup Runtime library names.
+  if (!useSoftFloat()) {
+    Info.setLibcallImpl(RTLIB::ADD_F128, RTLIB::impl__Q_add);
+    Info.setLibcallImpl(RTLIB::SUB_F128, RTLIB::impl__Q_sub);
+    Info.setLibcallImpl(RTLIB::MUL_F128, RTLIB::impl__Q_mul);
+    Info.setLibcallImpl(RTLIB::DIV_F128, RTLIB::impl__Q_div);
+    Info.setLibcallImpl(RTLIB::SQRT_F128, RTLIB::impl__Q_sqrt);
+    Info.setLibcallImpl(RTLIB::FPTOSINT_F128_I32, RTLIB::impl__Q_qtoi);
+    Info.setLibcallImpl(RTLIB::FPTOUINT_F128_I32, RTLIB::impl__Q_qtou);
+    Info.setLibcallImpl(RTLIB::SINTTOFP_I32_F128, RTLIB::impl__Q_itoq);
+    Info.setLibcallImpl(RTLIB::UINTTOFP_I32_F128, RTLIB::impl__Q_utoq);
+    Info.setLibcallImpl(RTLIB::FPEXT_F32_F128, RTLIB::impl__Q_stoq);
+    Info.setLibcallImpl(RTLIB::FPEXT_F64_F128, RTLIB::impl__Q_dtoq);
+    Info.setLibcallImpl(RTLIB::FPROUND_F128_F32, RTLIB::impl__Q_qtos);
+    Info.setLibcallImpl(RTLIB::FPROUND_F128_F64, RTLIB::impl__Q_qtod);
+  }
+}
+
+int YaRISCSubtarget::getAdjustedFrameSize(int frameSize) const {
+  // Emit the correct save instruction based on the number of bytes in
+  // the frame. Minimum stack frame size according to V8 ABI is:
+  //   16 words for register window spill
+  //    1 word for address of returned aggregate-value
+  // +  6 words for passing parameters on the stack
+  // ----------
+  //   23 words * 4 bytes per word = 92 bytes
+  frameSize += 92;
+
+  // Round up to next doubleword boundary -- a double-word boundary
+  // is required by the ABI.
+  frameSize = alignTo(frameSize, 8);
+  return frameSize;
+}
+
+bool YaRISCSubtarget::enableMachineScheduler() const { return true; }
diff --git a/llvm/lib/Target/YaRISC/YaRISCSubtarget.h b/llvm/lib/Target/YaRISC/YaRISCSubtarget.h
new file mode 100644
index 000000000000..3f193dc615b8
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCSubtarget.h
@@ -0,0 +1,97 @@
+//===-- YaRISCSubtarget.h - Subtarget for the YaRISC ------------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file declares the YaRISC specific subclass of TargetSubtargetInfo.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_YARISCSUBTARGET_H
+#define LLVM_LIB_TARGET_YARISC_YARISCSUBTARGET_H
+
+#include "MCTargetDesc/YaRISCMCTargetDesc.h"
+#include "YaRISCFrameLowering.h"
+#include "YaRISCISelLowering.h"
+#include "YaRISCInstrInfo.h"
+#include "llvm/CodeGen/TargetSubtargetInfo.h"
+#include "llvm/IR/DataLayout.h"
+#include "llvm/Support/ErrorHandling.h"
+#include "llvm/TargetParser/Triple.h"
+
+#define GET_SUBTARGETINFO_HEADER
+#include "YaRISCGenSubtargetInfo.inc"
+
+namespace llvm {
+class StringRef;
+
+class YaRISCSubtarget : public YaRISCGenSubtargetInfo {
+  // ReserveRegister[i] - Register #i is not available as a general purpose
+  // register.
+  BitVector ReserveRegister;
+
+  virtual void anchor();
+
+#define GET_SUBTARGETINFO_MACRO(ATTRIBUTE, DEFAULT, GETTER)                    \
+  bool ATTRIBUTE = DEFAULT;
+#include "YaRISCGenSubtargetInfo.inc"
+
+  YaRISCInstrInfo InstrInfo;
+  YaRISCTargetLowering TLInfo;
+  std::unique_ptr<const SelectionDAGTargetInfo> TSInfo;
+  YaRISCFrameLowering FrameLowering;
+
+public:
+  YaRISCSubtarget(const StringRef &CPU, const StringRef &TuneCPU,
+                  const StringRef &FS, const TargetMachine &TM);
+
+  ~YaRISCSubtarget() override;
+
+  const YaRISCInstrInfo *getInstrInfo() const override { return &InstrInfo; }
+  const TargetFrameLowering *getFrameLowering() const override {
+    return &FrameLowering;
+  }
+  const YaRISCRegisterInfo *getRegisterInfo() const override {
+    return &InstrInfo.getRegisterInfo();
+  }
+  const YaRISCTargetLowering *getTargetLowering() const override {
+    return &TLInfo;
+  }
+
+  const SelectionDAGTargetInfo *getSelectionDAGInfo() const override;
+
+  void initLibcallLoweringInfo(LibcallLoweringInfo &Info) const override;
+
+  bool enableMachineScheduler() const override;
+
+#define GET_SUBTARGETINFO_MACRO(ATTRIBUTE, DEFAULT, GETTER)                    \
+  bool GETTER() const { return ATTRIBUTE; }
+#include "YaRISCGenSubtargetInfo.inc"
+
+  /// ParseSubtargetFeatures - Parses features string setting specified
+  /// subtarget options.  Definition of function is auto generated by tblgen.
+  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);
+  YaRISCSubtarget &initializeSubtargetDependencies(StringRef CPU,
+                                                   StringRef TuneCPU,
+                                                   StringRef FS);
+
+  /// The 64-bit ABI uses biased stack and frame pointers, so the stack frame
+  /// of the current function is the area from [%sp+BIAS] to [%fp+BIAS].
+  int64_t getStackPointerBias() const { return 0; }
+
+  bool isRegisterReserved(MCPhysReg PhysReg) const {
+    return ReserveRegister[PhysReg];
+  }
+
+  /// Given a actual stack size as determined by FrameInfo, this function
+  /// returns adjusted framesize which includes space for register window
+  /// spills and arguments.
+  int getAdjustedFrameSize(int stackSize) const;
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_YARISCSUBTARGET_H
diff --git a/llvm/lib/Target/YaRISC/YaRISCTargetMachine.cpp b/llvm/lib/Target/YaRISC/YaRISCTargetMachine.cpp
new file mode 100644
index 000000000000..d91dad5a4ddc
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCTargetMachine.cpp
@@ -0,0 +1,138 @@
+//===-- YaRISCTargetMachine.cpp - TargetMachine for YaRISC ----------------===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file implements the YaRISC specific subclass of TargetMachine.
+//
+//===----------------------------------------------------------------------===//
+
+#include "YaRISCTargetMachine.h"
+#include "TargetInfo/YaRISCTargetInfo.h"
+#include "YaRISC.h"
+#include "llvm/CodeGen/Passes.h"
+#include "llvm/CodeGen/TargetLoweringObjectFileImpl.h"
+#include "llvm/CodeGen/TargetPassConfig.h"
+#include "llvm/MC/TargetRegistry.h"
+#include "llvm/Support/Compiler.h"
+#include <optional>
+
+using namespace llvm;
+
+extern "C" LLVM_ABI LLVM_EXTERNAL_VISIBILITY void LLVMInitializeYaRISCTarget() {
+  // Register the target.
+  RegisterTargetMachine<YaRISCTargetMachine> X(getTheYaRISCTarget());
+
+  PassRegistry &PR = *PassRegistry::getPassRegistry();
+  initializeYaRISCAsmPrinterPass(PR);
+  initializeYaRISCDAGToDAGISelLegacyPass(PR);
+}
+
+static Reloc::Model getEffectiveRelocModel(std::optional<Reloc::Model> RM) {
+  if (!RM)
+    return Reloc::Static;
+
+  if (*RM == Reloc::PIC_) {
+    // The call lowering currently expects absolute addresses. No PC-relative
+    // addressing is implemented.
+    report_fatal_error("PIC is currently not supported", false);
+  }
+
+  return *RM;
+}
+
+static CodeModel::Model
+getEffectiveYaRISCCodeModel(std::optional<CodeModel::Model> CM,
+                            Reloc::Model RM) {
+  if (CM) {
+    if (*CM == CodeModel::Tiny)
+      report_fatal_error("Target does not support the tiny CodeModel", false);
+    if (*CM == CodeModel::Kernel)
+      report_fatal_error("Target does not support the kernel CodeModel", false);
+    return *CM;
+  }
+  return CodeModel::Small;
+}
+
+void YaRISCTargetMachine::anchor() {}
+
+YaRISCTargetMachine::YaRISCTargetMachine(const Target &T, const Triple &TT,
+                                         StringRef CPU, StringRef FS,
+                                         const TargetOptions &Options,
+                                         std::optional<Reloc::Model> RM,
+                                         std::optional<CodeModel::Model> CM,
+                                         CodeGenOptLevel OL, bool JIT)
+    : CodeGenTargetMachineImpl(
+          T, TT.computeDataLayout(), TT, CPU, FS, Options,
+          getEffectiveRelocModel(RM),
+          getEffectiveYaRISCCodeModel(CM, getEffectiveRelocModel(RM)), OL),
+      TLOF(std::make_unique<TargetLoweringObjectFileELF>()) {
+  initAsmInfo();
+}
+
+YaRISCTargetMachine::~YaRISCTargetMachine() = default;
+
+const YaRISCSubtarget *
+YaRISCTargetMachine::getSubtargetImpl(const Function &F) const {
+  Attribute CPUAttr = F.getFnAttribute("target-cpu");
+  Attribute TuneAttr = F.getFnAttribute("tune-cpu");
+  Attribute FSAttr = F.getFnAttribute("target-features");
+
+  std::string CPU =
+      CPUAttr.isValid() ? CPUAttr.getValueAsString().str() : TargetCPU;
+  std::string TuneCPU =
+      TuneAttr.isValid() ? TuneAttr.getValueAsString().str() : CPU;
+  std::string FS =
+      FSAttr.isValid() ? FSAttr.getValueAsString().str() : TargetFS;
+
+  // FIXME: This is related to the code below to reset the target options,
+  // we need to know whether or not the soft float flag is set on the
+  // function, so we can enable it as a subtarget feature.
+  bool softFloat = F.getFnAttribute("use-soft-float").getValueAsBool();
+
+  if (softFloat)
+    FS += FS.empty() ? "+soft-float" : ",+soft-float";
+
+  auto &I = SubtargetMap[CPU + FS];
+  if (!I) {
+    // This needs to be done before we create a new subtarget since any
+    // creation will depend on the TM and the code generation flags on the
+    // function that reside in TargetOptions.
+    resetTargetOptions(F);
+    I = std::make_unique<YaRISCSubtarget>(CPU, TuneCPU, FS, *this);
+  }
+  return I.get();
+}
+
+namespace {
+/// YaRISC Code Generator Pass Configuration Options.
+class YaRISCPassConfig : public TargetPassConfig {
+public:
+  YaRISCPassConfig(YaRISCTargetMachine &TM, PassManagerBase &PM)
+      : TargetPassConfig(TM, PM) {}
+
+  YaRISCTargetMachine &getYaRISCTargetMachine() const {
+    return getTM<YaRISCTargetMachine>();
+  }
+
+  bool addInstSelector() override;
+  void addPreEmitPass() override;
+};
+} // namespace
+
+TargetPassConfig *YaRISCTargetMachine::createPassConfig(PassManagerBase &PM) {
+  return new YaRISCPassConfig(*this, PM);
+}
+
+bool YaRISCPassConfig::addInstSelector() {
+  addPass(createYaRISCISelDag(getYaRISCTargetMachine()));
+  return false;
+}
+
+void YaRISCPassConfig::addPreEmitPass() {
+  // Check and replace branches when they become "too far".
+  addPass(&BranchRelaxationPassID);
+}
diff --git a/llvm/lib/Target/YaRISC/YaRISCTargetMachine.h b/llvm/lib/Target/YaRISC/YaRISCTargetMachine.h
new file mode 100644
index 000000000000..a8df1a464949
--- /dev/null
+++ b/llvm/lib/Target/YaRISC/YaRISCTargetMachine.h
@@ -0,0 +1,49 @@
+//===-- YaRISCTargetMachine.h - TargetMachine for YaRISC --------*- C++ -*-===//
+//
+// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file declares the YaRISC specific subclass of TargetMachine.
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_YARISC_YARISCTARGETMACHINE_H
+#define LLVM_LIB_TARGET_YARISC_YARISCTARGETMACHINE_H
+
+#include "YaRISCInstrInfo.h"
+#include "YaRISCSubtarget.h"
+#include "llvm/CodeGen/CodeGenTargetMachineImpl.h"
+#include "llvm/Target/TargetMachine.h"
+#include <optional>
+
+namespace llvm {
+
+class YaRISCTargetMachine : public CodeGenTargetMachineImpl {
+  std::unique_ptr<TargetLoweringObjectFile> TLOF;
+  mutable StringMap<std::unique_ptr<YaRISCSubtarget>> SubtargetMap;
+
+  virtual void anchor();
+
+public:
+  YaRISCTargetMachine(const Target &T, const Triple &TT, StringRef CPU,
+                      StringRef FS, const TargetOptions &Options,
+                      std::optional<Reloc::Model> RM,
+                      std::optional<CodeModel::Model> CM, CodeGenOptLevel OL,
+                      bool JIT);
+  ~YaRISCTargetMachine() override;
+
+  const YaRISCSubtarget *getSubtargetImpl(const Function &F) const override;
+
+  // Pass Pipeline Configuration
+  TargetPassConfig *createPassConfig(PassManagerBase &PM) override;
+  TargetLoweringObjectFile *getObjFileLowering() const override {
+    return TLOF.get();
+  }
+};
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_YARISC_YARISCTARGETMACHINE_H
diff --git a/llvm/lib/TargetParser/TargetDataLayout.cpp b/llvm/lib/TargetParser/TargetDataLayout.cpp
index 981c5561211d..4faa844609f6 100644
--- a/llvm/lib/TargetParser/TargetDataLayout.cpp
+++ b/llvm/lib/TargetParser/TargetDataLayout.cpp
@@ -602,6 +602,8 @@ std::string Triple::computeDataLayout(StringRef ABIName) const {
     return "e-m:e-p:32:32-i1:8:32-i8:8:32-i16:16:32-i64:32-f64:32-a:0:32-n32";
   case Triple::xtensa:
     return "e-m:e-p:32:32-i8:8:32-i16:16:32-i64:64-n32";
+  case Triple::yarisc:
+    return "e-m:e-p:16:16-i16:16:16-i32:16-i64:16-f32:16-f64:16-a:8-n16-S16";
   case Triple::nvptx:
   case Triple::nvptx64:
     return computeNVPTXDataLayout(*this, ABIName);
diff --git a/llvm/lib/TargetParser/Triple.cpp b/llvm/lib/TargetParser/Triple.cpp
index 2577b15b1999..7ff7b7d5e072 100644
--- a/llvm/lib/TargetParser/Triple.cpp
+++ b/llvm/lib/TargetParser/Triple.cpp
@@ -88,6 +88,7 @@ StringRef Triple::getArchTypeName(ArchType Kind) {
   case x86_64:         return "x86_64";
   case xcore:          return "xcore";
   case xtensa:         return "xtensa";
+  case yarisc:         return "yarisc";
   }
 
   llvm_unreachable("Invalid ArchType!");
@@ -259,6 +260,8 @@ StringRef Triple::getArchTypePrefix(ArchType Kind) {
   case dxil:        return "dx";
 
   case xtensa:      return "xtensa";
+
+  case yarisc:      return "yarisc";
   }
 }
 
@@ -512,6 +515,7 @@ Triple::ArchType Triple::getArchTypeForLLVMName(StringRef Name) {
       .Case("loongarch64", loongarch64)
       .Case("dxil", dxil)
       .Case("xtensa", xtensa)
+      .Case("yarisc", yarisc)
       .Default(UnknownArch);
 }
 
@@ -664,6 +668,7 @@ static Triple::ArchType parseArch(StringRef ArchName) {
                   "dxilv1.9"},
                  Triple::dxil)
           .Case("xtensa", Triple::xtensa)
+          .Case("yarisc", Triple::yarisc)
           .Default(Triple::UnknownArch);
 
   // Some architectures require special parsing logic just to compute the
@@ -1019,6 +1024,7 @@ static Triple::ObjectFormatType getDefaultFormat(const Triple &T) {
   case Triple::ve:
   case Triple::xcore:
   case Triple::xtensa:
+  case Triple::yarisc:
     return Triple::ELF;
 
   case Triple::mipsel:
@@ -1708,6 +1714,7 @@ unsigned Triple::getArchPointerBitWidth(llvm::Triple::ArchType Arch) {
 
   case llvm::Triple::avr:
   case llvm::Triple::msp430:
+  case llvm::Triple::yarisc:
     return 16;
 
   case llvm::Triple::aarch64_32:
@@ -1817,6 +1824,7 @@ Triple Triple::get32BitArchVariant() const {
   case Triple::msp430:
   case Triple::systemz:
   case Triple::ve:
+  case Triple::yarisc:
     T.setArch(UnknownArch);
     break;
 
@@ -1909,6 +1917,7 @@ Triple Triple::get64BitArchVariant() const {
   case Triple::tcele:
   case Triple::xcore:
   case Triple::xtensa:
+  case Triple::yarisc:
     T.setArch(UnknownArch);
     break;
 
@@ -2010,6 +2019,7 @@ Triple Triple::getBigEndianArchVariant() const {
   case Triple::ve:
   case Triple::csky:
   case Triple::xtensa:
+  case Triple::yarisc:
 
   // ARM is intentionally unsupported here, changing the architecture would
   // drop any arch suffixes.
@@ -2131,6 +2141,7 @@ bool Triple::isLittleEndian() const {
   case Triple::x86_64:
   case Triple::xcore:
   case Triple::xtensa:
+  case Triple::yarisc:
     return true;
   default:
     return false;
@@ -2355,6 +2366,8 @@ ExceptionHandling Triple::getDefaultExceptionHandling() const {
   case Triple::xcore:
   case Triple::xtensa:
     return ExceptionHandling::DwarfCFI;
+  case Triple::yarisc:
+    return ExceptionHandling::None;
   default:
     break;
   }
